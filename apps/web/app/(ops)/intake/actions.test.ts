import { describe, it } from 'vitest'

// Wave 0 stubs — ops intake Server Action tests
// These todos map to INTAKE-07 and will be implemented in later Phase 5 plans.

describe('overrideQuarantine', () => {
  it.todo('overrideQuarantine rejects empty reason (< 10 chars)') // INTAKE-07
  it.todo('overrideQuarantine sets quarantine_overridden=true with reason') // INTAKE-07
})
