---
phase: 05-prison-intake-and-counting
plan: 07
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/app/(ops)/intake/page.tsx
  - apps/web/app/(ops)/intake/components/intake-queue-table.tsx
  - apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx
  - apps/web/app/(ops)/intake/actions.ts
autonomous: true
requirements:
  - INTAKE-02
  - INTAKE-05

must_haves:
  truths:
    - "Ops portal intake queue shows all intake records with status tabs"
    - "Unexpected deliveries have a visible badge in the queue"
    - "Quarantine queue section shows blocked records with Override and Allow button"
    - "Override dialog requires reason (min 10 chars) before allowing override"
  artifacts:
    - path: "apps/web/app/(ops)/intake/page.tsx"
      provides: "Intake queue with tabs: All, Discrepancy Flagged, Quarantine Blocked, Unexpected"
      contains: "Tabs"
    - path: "apps/web/app/(ops)/intake/components/intake-queue-table.tsx"
      provides: "Intake records table component"
      exports: ["IntakeQueueTable"]
    - path: "apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx"
      provides: "Override dialog with reason textarea"
      exports: ["QuarantineOverrideDialog"]
  key_links:
    - from: "apps/web/app/(ops)/intake/page.tsx"
      to: "apps/web/app/(ops)/intake/actions.ts"
      via: "getIntakeQueue, getQuarantinedIntakes"
      pattern: "getIntakeQueue|getQuarantinedIntakes"
---

<objective>
Build the ops portal intake queue page with status tabs (All, Discrepancy Flagged, Quarantine Blocked, Unexpected), the intake records table, and the quarantine override dialog with reason textarea.

Purpose: reco-admin can monitor all intake activity, review discrepancies, manage quarantine overrides, and spot unexpected deliveries.
Output: Working ops intake queue at /(ops)/intake with full tab filtering and quarantine override UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-prison-intake-and-counting/05-CONTEXT.md
@.planning/phases/05-prison-intake-and-counting/05-UI-SPEC.md
@.planning/phases/05-prison-intake-and-counting/05-01-SUMMARY.md

<interfaces>
From apps/web/app/(ops)/pickups/page.tsx (existing pattern):
```typescript
// Pickup queue uses shadcn Tabs + Table pattern with status filter tabs
// Server Component page fetches data, renders Client Component table
```

From apps/web/app/(ops)/intake/actions.ts (Plan 06 creates overrideQuarantine, getQuarantinedIntakes):
```typescript
export async function overrideQuarantine(intakeRecordId: string, reason: string): Promise<{ success: boolean } | { error: string }>
export async function getQuarantinedIntakes(): Promise<QuarantinedIntake[]>
```

From packages/db/src/schema/intake.ts (Plan 01):
```typescript
export const intakeRecords = pgTable('intake_records', { id, reference, prison_facility_id, tenant_id, staff_name, delivery_date, is_unexpected, discrepancy_flagged, quarantine_flagged, quarantine_overridden, ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create getIntakeQueue Server Action and intake queue page</name>
  <read_first>
    - apps/web/app/(ops)/pickups/page.tsx (pickup queue page pattern to replicate)
    - apps/web/app/(ops)/pickups/actions.ts (getPickupQueue pattern — tabs + table)
    - apps/web/app/(ops)/intake/actions.ts (existing overrideQuarantine, getQuarantinedIntakes if already created by Plan 06; if not yet, create stubs)
    - packages/db/src/schema/intake.ts (intakeRecords columns for query)
    - packages/db/src/schema/settings.ts (prisonFacilities for facility name join)
    - apps/web/components/ui/table.tsx (shadcn Table)
    - apps/web/components/ui/tabs.tsx (shadcn Tabs)
    - apps/web/components/ui/badge.tsx (for unexpected delivery badge)
  </read_first>
  <files>
    apps/web/app/(ops)/intake/page.tsx,
    apps/web/app/(ops)/intake/components/intake-queue-table.tsx,
    apps/web/app/(ops)/intake/actions.ts
  </files>
  <action>
    **Add to `apps/web/app/(ops)/intake/actions.ts`** (create file if Plan 06 hasn't run yet; extend if it has):

    `getIntakeQueue(filter?: 'all' | 'discrepancy' | 'quarantine' | 'unexpected')`:
    - Call requireRecoAdmin()
    - Query intake_records via withRLSContext
    - JOIN prison_facilities (for facility_name), tenants (for client_name)
    - Apply filter:
      - 'all': no filter
      - 'discrepancy': WHERE discrepancy_flagged = true
      - 'quarantine': WHERE quarantine_flagged = true AND quarantine_overridden = false
      - 'unexpected': WHERE is_unexpected = true
    - ORDER BY created_at DESC
    - Return: `{ id, reference, facility_name, client_name, staff_name, delivery_date, origin_market, is_unexpected, discrepancy_flagged, quarantine_flagged, quarantine_overridden, created_at }[]`

    **Create `apps/web/app/(ops)/intake/components/intake-queue-table.tsx`** ('use client'):
    - Props: `intakes: IntakeQueueItem[], onOverride?: (id: string) => void`
    - shadcn Table with columns: Reference, Facility, Client, Staff, Date, Market, Status badges, Actions
    - Status badges:
      - Unexpected: shadcn Badge "Unexpected" (outline variant)
      - Discrepancy: shadcn Badge "Discrepancy" (amber variant — `className="bg-amber-50 text-amber-700 border-amber-200"`)
      - Quarantine Blocked: shadcn Badge "Quarantine" (destructive variant)
      - Quarantine Overridden: shadcn Badge "Overridden" (muted variant)
    - Actions column: "Override" button on quarantine-blocked rows (triggers override dialog)

    **Create `apps/web/app/(ops)/intake/page.tsx`** (Server Component):
    - shadcn Tabs with 4 tabs: All, Discrepancy Flagged, Quarantine Blocked, Unexpected
    - Each tab renders IntakeQueueTable with the appropriate filter
    - Use URL search params for tab state (e.g. ?tab=quarantine) to enable deep linking
    - Empty state per UI-SPEC: "No intake records yet. Expected deliveries appear here once transport is marked delivered."
    - Wrap in requireAuth/requireRecoAdmin guard
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && pnpm --filter @repo/web test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File apps/web/app/(ops)/intake/page.tsx exists
    - grep "Tabs\|tabs" apps/web/app/(ops)/intake/page.tsx returns matches
    - grep "getIntakeQueue" apps/web/app/(ops)/intake/actions.ts returns a match
    - File apps/web/app/(ops)/intake/components/intake-queue-table.tsx exists
    - grep "Table\|table" apps/web/app/(ops)/intake/components/intake-queue-table.tsx returns matches
    - grep "Unexpected\|unexpected" apps/web/app/(ops)/intake/components/intake-queue-table.tsx returns matches (badge)
    - grep "Quarantine\|quarantine" apps/web/app/(ops)/intake/components/intake-queue-table.tsx returns matches
    - pnpm --filter @repo/web test passes
  </acceptance_criteria>
  <done>Ops intake queue page shows all intake records with 4 status tabs; badges indicate unexpected, discrepancy, quarantine status</done>
</task>

<task type="auto">
  <name>Task 2: Create quarantine override dialog component</name>
  <read_first>
    - apps/web/app/(ops)/intake/actions.ts (overrideQuarantine action)
    - apps/web/components/ui/dialog.tsx (shadcn Dialog pattern)
    - apps/web/app/(ops)/intake/components/intake-queue-table.tsx (where override button triggers dialog)
    - .planning/phases/05-prison-intake-and-counting/05-UI-SPEC.md (override dialog spec: 10 char minimum)
  </read_first>
  <files>
    apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx
  </files>
  <action>
    **Create `apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx`** ('use client'):
    - Props: `intakeRecordId: string, open: boolean, onOpenChange: (open: boolean) => void`
    - shadcn Dialog (or @base-ui Dialog — check which is used in existing codebase) with:
      - Title: "Override Quarantine"
      - Description: "This intake record is quarantine-blocked due to a batch flag match. Provide a reason to override."
      - Textarea for reason (required, min 10 characters)
      - Character count display: "{n}/10 minimum"
      - Confirm button: "Approve and Release" (per UI-SPEC English ops label) — disabled until textarea has >= 10 chars
      - Cancel button: "Cancel"
    - On confirm: call overrideQuarantine(intakeRecordId, reason)
    - On success: close dialog, show sonner toast "Quarantine override approved", trigger page refresh (revalidatePath or router.refresh)
    - On error: show error in dialog (reason too short, or server error)

    Wire the dialog into IntakeQueueTable: when "Override" button is clicked on a quarantine-blocked row, open QuarantineOverrideDialog with that row's ID.
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && pnpm --filter @repo/web test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx exists with 'use client'
    - grep "overrideQuarantine" apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx returns a match
    - grep "10" apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx returns a match (min chars)
    - grep "disabled" apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx returns a match
    - grep "Approve and Release\|Override" apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx returns a match
    - grep "textarea\|Textarea" apps/web/app/(ops)/intake/components/quarantine-override-dialog.tsx returns a match
    - pnpm --filter @repo/web test passes
  </acceptance_criteria>
  <done>Quarantine override dialog requires 10+ char reason; calls overrideQuarantine on confirm; shows success toast and refreshes queue</done>
</task>

</tasks>

<verification>
- /(ops)/intake shows intake queue with 4 tabs
- Quarantine Blocked tab shows blocked records with Override button
- Clicking Override opens dialog with required reason textarea
- Override succeeds with 10+ char reason; fails with shorter
- Unexpected deliveries show badge in queue
</verification>

<success_criteria>
- Intake queue table displays with correct tab filtering
- Quarantine override dialog enforces 10-char minimum reason
- Unexpected delivery badge visible in queue
- All ops nav accessible via Intake link
</success_criteria>

<output>
After completion, create `.planning/phases/05-prison-intake-and-counting/05-07-SUMMARY.md`
</output>
