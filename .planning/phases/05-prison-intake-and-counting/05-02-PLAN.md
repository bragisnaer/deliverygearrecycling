---
phase: 05-prison-intake-and-counting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/prison/layout.tsx
  - apps/web/app/prison/page.tsx
  - apps/web/messages/da.json
  - apps/web/i18n/request.ts
  - apps/web/next.config.ts
  - apps/web/app/(ops)/ops-nav-bar.tsx
autonomous: true
requirements:
  - INTAKE-01

must_haves:
  truths:
    - "Prison staff at a pre-authenticated tablet reach the home screen without login credentials"
    - "Prison home screen displays in Danish with Registrer Levering primary CTA"
    - "next-intl is installed and configured for Danish locale on prison routes only"
    - "Ops nav bar includes Intake link"
  artifacts:
    - path: "apps/web/app/prison/layout.tsx"
      provides: "Prison tablet shell layout with next-intl provider and facility gate"
      contains: "NextIntlClientProvider"
    - path: "apps/web/app/prison/page.tsx"
      provides: "Prison home screen with primary CTA and history tab"
      contains: "Registrer Levering"
    - path: "apps/web/messages/da.json"
      provides: "Danish translation strings for prison tablet"
      contains: "intake"
    - path: "apps/web/i18n/request.ts"
      provides: "next-intl server config"
      contains: "getRequestConfig"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "apps/web/i18n/request.ts"
      via: "createNextIntlPlugin"
      pattern: "createNextIntlPlugin"
    - from: "apps/web/app/prison/layout.tsx"
      to: "apps/web/messages/da.json"
      via: "getMessages"
      pattern: "getMessages"
---

<objective>
Install next-intl, create the prison tablet shell layout with Danish i18n, build the prison home screen with the primary CTA and history tab placeholder, and add the Intake nav item to the ops portal.

Purpose: Establishes the prison tablet interface shell that all subsequent prison-facing plans render inside, and wires up Danish i18n that all prison UI strings depend on.
Output: Working prison tablet shell at /prison/ with Danish labels, next-intl configured, ops nav updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prison-intake-and-counting/05-CONTEXT.md
@.planning/phases/05-prison-intake-and-counting/05-RESEARCH.md
@.planning/phases/05-prison-intake-and-counting/05-UI-SPEC.md

<interfaces>
From apps/web/app/prison/login/ (existing):
```
Prison login page already exists from Phase 2. The prison layout.tsx wraps all /prison/ routes.
```

From apps/web/app/(ops)/ops-nav-bar.tsx:
```typescript
const NAV_ITEMS = [
  { label: 'Dashboard', href: '/dashboard' },
  { label: 'Pickups', href: '/pickups' },
  { label: 'Transport', href: '/transport' },
  { label: 'Products', href: '/products' },
  { label: 'Settings', href: '/settings' },
]
export function OpsNavBar() { ... }
```

From apps/web/next.config.ts (current):
```typescript
// Standard Next.js 16 config — needs createNextIntlPlugin wrapper added
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-intl and configure i18n for prison routes</name>
  <read_first>
    - apps/web/next.config.ts (current config to wrap with createNextIntlPlugin)
    - apps/web/package.json (verify next-intl not already installed)
    - apps/web/app/prison/login/page.tsx (existing prison route structure)
  </read_first>
  <files>
    apps/web/i18n/request.ts,
    apps/web/messages/da.json,
    apps/web/next.config.ts
  </files>
  <action>
    **Step 1: Install next-intl:**
    ```bash
    pnpm --filter @repo/web add next-intl
    ```

    **Step 2: Install new shadcn components needed this phase:**
    ```bash
    cd apps/web && npx shadcn@latest add select skeleton alert
    ```

    **Step 3: Create `apps/web/i18n/request.ts`:**
    ```typescript
    import { getRequestConfig } from 'next-intl/server'

    export default getRequestConfig(async () => {
      return {
        locale: 'da',
        messages: (await import('../messages/da.json')).default,
      }
    })
    ```

    **Step 4: Create `apps/web/messages/da.json`** with all Danish strings from UI-SPEC.md:
    ```json
    {
      "intake": {
        "home": {
          "primary_cta": "Registrer Levering",
          "history_tab": "Historik",
          "deliveries_tab": "Forventede Leveringer",
          "no_deliveries": "Ingen forventede leveringer i dag. Brug knappen nedenfor for at registrere en uventet levering.",
          "no_history": "Ingen registrerede leveringer i de seneste 30 dage."
        },
        "form": {
          "submit": "Indsend Optaelling",
          "staff_name": "Medarbejdernavn",
          "delivery_date": "Leveringsdato",
          "client": "Klient",
          "origin_market": "Oprindelsesmarked",
          "batch_lot_number": "Batch/Lot Nummer",
          "notes": "Noter",
          "photos": "Fotos",
          "quantity": "Antal",
          "expected": "Forventet",
          "increase_qty": "Foroeg antal",
          "decrease_qty": "Reducer antal",
          "product": "Produkt",
          "select_client": "Vaelg klient",
          "unexpected_delivery": "Uventet Levering",
          "register_unexpected": "Registrer Uventet Levering"
        },
        "discrepancy": {
          "above": "Forventet: {expected}. Du indtastede {actual} (+{pct}% over forventet)",
          "below": "Forventet: {expected}. Du indtastede {actual} (-{pct}% under forventet)",
          "summary": "En eller flere linjer afviger fra de forventede maengder. Optaellingen indsendes, og reco-admin underrettes automatisk.",
          "from_expected": "fra forventet"
        },
        "quarantine": {
          "blocked": "Denne levering er karantaenaespaerret paa grund af et batchmatch. En reco-administrator skal godkende, inden du kan fortsaette.",
          "override_label": "Godkend og frigiv"
        },
        "success": {
          "title": "Levering Registreret",
          "register_another": "Registrer en anden levering",
          "go_home": "Tilbage til forsiden"
        },
        "errors": {
          "submission_failed": "Indsendelse mislykkedes. Tjek din forbindelse og proev igen.",
          "client_load_failed": "Kunne ikke indlaese klientlisten. Genindlaes siden."
        }
      }
    }
    ```
    NOTE: Use ae, oe, aa for Danish special chars (ae=ae, oe=oe, aa=aa) — the actual file should use proper Unicode: ae -> \u00e6, oe -> \u00f8, aa -> \u00e5. Write the file with proper Danish characters: Optaelling -> Opt\u00e6lling, Foroeg -> For\u00f8g, etc.

    **Step 5: Update `apps/web/next.config.ts`** to wrap the existing config with createNextIntlPlugin:
    ```typescript
    import createNextIntlPlugin from 'next-intl/plugin'
    const withNextIntl = createNextIntlPlugin('./i18n/request.ts')
    // wrap existing export: export default withNextIntl(existingConfig)
    ```
    Preserve all existing config. Only add the wrapper.

    **Step 6: Verify build succeeds:**
    ```bash
    cd apps/web && npx next build 2>&1 | tail -20
    ```
    If next-intl plugin causes issues with Next.js 16.2.0 or Turbopack, fall back to a manual provider without the plugin (import messages directly in layout). Document in SUMMARY.
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && pnpm --filter @repo/web test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "next-intl" apps/web/package.json returns a match
    - File apps/web/i18n/request.ts exists
    - grep "getRequestConfig" apps/web/i18n/request.ts returns a match
    - File apps/web/messages/da.json exists
    - grep "Registrer Levering" apps/web/messages/da.json returns a match (or the proper Danish string)
    - grep "createNextIntlPlugin" apps/web/next.config.ts returns a match
    - pnpm --filter @repo/web test passes (no regressions)
  </acceptance_criteria>
  <done>next-intl installed and configured; Danish messages file created; next.config.ts wrapped with plugin; shadcn select/skeleton/alert installed</done>
</task>

<task type="auto">
  <name>Task 2: Create prison tablet shell layout and home screen + ops nav update</name>
  <read_first>
    - apps/web/app/prison/login/page.tsx (existing prison route to understand auth pattern)
    - apps/web/app/(ops)/layout.tsx (ops layout pattern for comparison)
    - apps/web/app/(ops)/ops-nav-bar.tsx (nav items array to add Intake)
    - apps/web/app/globals.css (brand tokens)
    - .planning/phases/05-prison-intake-and-counting/05-UI-SPEC.md (tablet shell spec, navigation structure)
  </read_first>
  <files>
    apps/web/app/prison/layout.tsx,
    apps/web/app/prison/page.tsx,
    apps/web/app/(ops)/ops-nav-bar.tsx
  </files>
  <action>
    **Create/Update `apps/web/app/prison/layout.tsx`:**
    - If layout.tsx already exists (from Phase 2 login), extend it. If not, create it.
    - Import `NextIntlClientProvider` from 'next-intl' and `getMessages` from 'next-intl/server'
    - Gate: call `auth()` and verify `session.user.role === 'prison'`; if not, redirect to `/prison/login`
    - Extract `facility_id` and facility name from session for display
    - Shell layout: full-viewport, no sidebar, single top bar with:
      - reco logo (left)
      - Facility name (centre, from session)
      - No user controls (per UI-SPEC: facility-pre-authenticated, no logout)
    - Wrap children in `<NextIntlClientProvider locale="da" messages={messages}>`
    - Use `bg-background` for the shell background (#FAF9F4)
    - Minimum touch target 44px enforced via Tailwind utility classes on interactive elements

    **Create `apps/web/app/prison/page.tsx`:**
    Prison home screen with:
    - Large primary CTA card: "Registrer Levering" button (red fill, min 48px height, Consolas font) linking to `/prison/incoming`
    - Below: two tabs using shadcn Tabs — "Forventede Leveringer" (default active) and "Historik"
    - "Forventede Leveringer" tab: placeholder content "Loading deliveries..." (will be populated in Plan 03)
    - "Historik" tab: placeholder content showing empty state string from da.json
    - Use `useTranslations('intake.home')` for all string references
    - Page is a Server Component; tabs section extracted as Client Component if needed for interactivity

    **Update `apps/web/app/(ops)/ops-nav-bar.tsx`:**
    Add Intake to NAV_ITEMS array between Transport and Products:
    ```typescript
    const NAV_ITEMS = [
      { label: 'Dashboard', href: '/dashboard' },
      { label: 'Pickups', href: '/pickups' },
      { label: 'Transport', href: '/transport' },
      { label: 'Intake', href: '/intake' },
      { label: 'Products', href: '/products' },
      { label: 'Settings', href: '/settings' },
    ]
    ```
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && pnpm --filter @repo/web test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File apps/web/app/prison/layout.tsx exists
    - grep "NextIntlClientProvider" apps/web/app/prison/layout.tsx returns a match
    - grep "prison" apps/web/app/prison/layout.tsx returns matches (role check)
    - File apps/web/app/prison/page.tsx exists
    - grep "Registrer Levering" apps/web/app/prison/page.tsx returns a match OR grep "intake.home.primary_cta" (i18n key reference)
    - grep "Intake" apps/web/app/\(ops\)/ops-nav-bar.tsx returns a match
    - grep "/intake" apps/web/app/\(ops\)/ops-nav-bar.tsx returns a match
    - pnpm --filter @repo/web test passes (no regressions)
  </acceptance_criteria>
  <done>Prison tablet shell renders with Danish labels; home screen shows primary CTA and tab structure; ops nav includes Intake link</done>
</task>

</tasks>

<verification>
- Visiting /prison/ (when authenticated as prison role) shows the tablet shell with facility name and Danish CTA
- next-intl is loaded and da.json messages are accessible via useTranslations
- Ops portal nav bar shows "Intake" link pointing to /intake
- All existing tests pass
</verification>

<success_criteria>
- next-intl installed, configured, and working with Danish locale on prison routes
- Prison home screen displays "Registrer Levering" CTA and tab structure
- Ops nav bar includes Intake link
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-prison-intake-and-counting/05-02-SUMMARY.md`
</output>
