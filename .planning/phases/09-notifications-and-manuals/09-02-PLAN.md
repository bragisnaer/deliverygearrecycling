---
phase: 09-notifications-and-manuals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/emails/discrepancy-alert.tsx
  - apps/web/emails/uninvoiced-alert.tsx
  - apps/web/emails/defective-batch-alert.tsx
  - apps/web/emails/warehouse-ageing-alert.tsx
  - apps/web/emails/facility-inactive-alert.tsx
  - apps/web/emails/pickup-confirmed.tsx
autonomous: true
requirements:
  - NOTIF-04
must_haves:
  truths:
    - "Six new React Email templates exist for all notification matrix gaps"
    - "Each template follows the established PickupAdminAlertEmail pattern"
    - "Templates accept typed props and render with @react-email/components"
  artifacts:
    - path: "apps/web/emails/discrepancy-alert.tsx"
      provides: "Email template for discrepancy >15% events"
      exports: ["default"]
    - path: "apps/web/emails/uninvoiced-alert.tsx"
      provides: "Email template for uninvoiced >14 day events"
      exports: ["default"]
    - path: "apps/web/emails/defective-batch-alert.tsx"
      provides: "Email template for defective batch match events"
      exports: ["default"]
    - path: "apps/web/emails/warehouse-ageing-alert.tsx"
      provides: "Email template for warehouse ageing alerts"
      exports: ["default"]
    - path: "apps/web/emails/facility-inactive-alert.tsx"
      provides: "Email template for prison facility inactive >14 days"
      exports: ["default"]
    - path: "apps/web/emails/pickup-confirmed.tsx"
      provides: "Email template for pickup confirmation to client"
      exports: ["default"]
  key_links:
    - from: "apps/web/emails/*.tsx"
      to: "@react-email/components"
      via: "import { Body, Container, Head, ... }"
      pattern: "from '@react-email/components'"
---

<objective>
Create all six missing React Email templates identified in the PRD notification matrix gap analysis. These templates will be wired to event triggers in Plan 06.

Purpose: Complete the transactional email template set so all notification events can send email per NOTIF-04.
Output: Six new email template components in apps/web/emails/.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-notifications-and-manuals/09-RESEARCH.md

<interfaces>
<!-- Existing email template pattern from apps/web/emails/pickup-admin-alert.tsx -->
import {
  Body, Container, Head, Heading, Html, Link, Preview, Section, Text,
} from '@react-email/components'

interface PickupAdminAlertEmailProps {
  reference: string
  clientName: string
  // ... typed props
}

export default function PickupAdminAlertEmail(props: PickupAdminAlertEmailProps) {
  const platformUrl = process.env.NEXT_PUBLIC_APP_DOMAIN
    ? `https://${process.env.NEXT_PUBLIC_APP_DOMAIN}/...`
    : `/...`
  return (
    <Html><Head /><Preview>...</Preview>
      <Body style={{ fontFamily: 'sans-serif', backgroundColor: '#f6f6f6' }}>
        <Container style={{ maxWidth: '560px', margin: '0 auto', padding: '40px 20px' }}>
          ...
        </Container>
      </Body>
    </Html>
  )
}

<!-- Existing sendEmail pattern from apps/web/lib/email.ts -->
export async function sendEmail({ to, subject, react }: {
  to: string | string[]
  subject: string
  react: React.ReactElement
})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create critical event email templates (4 templates)</name>
  <files>
    apps/web/emails/discrepancy-alert.tsx
    apps/web/emails/uninvoiced-alert.tsx
    apps/web/emails/defective-batch-alert.tsx
    apps/web/emails/facility-inactive-alert.tsx
  </files>
  <read_first>
    apps/web/emails/pickup-admin-alert.tsx
    apps/web/emails/pickup-confirmation.tsx
    apps/web/lib/email.ts
  </read_first>
  <action>
Create four email templates following the exact same structure as `pickup-admin-alert.tsx`. Each uses `@react-email/components` imports (Body, Container, Head, Heading, Html, Link, Preview, Section, Text). Each has inline styles matching the existing pattern (fontFamily: sans-serif, backgroundColor: #f6f6f6, container maxWidth: 560px, etc.).

**1. `apps/web/emails/discrepancy-alert.tsx`:**

Props interface `DiscrepancyAlertEmailProps`:
- `intakeReference: string` — e.g. "IN-2026-0042"
- `facilityName: string` — prison facility name
- `clientName: string`
- `discrepancyPercent: number` — e.g. 23
- `intakeId: string` — for platform link

Default export `DiscrepancyAlertEmail`.

Preview: `Discrepancy detected: ${intakeReference} — ${discrepancyPercent}% variance`

Body content section:
- Heading: "Discrepancy Alert"
- Text: `Intake ${intakeReference} at ${facilityName} for ${clientName} has a discrepancy of ${discrepancyPercent}% — exceeding the configured threshold.`
- Section with key/value pairs: Reference, Facility, Client, Discrepancy
- "View in platform" link to `/intake/${intakeId}`

**2. `apps/web/emails/uninvoiced-alert.tsx`:**

Props interface `UninvoicedAlertEmailProps`:
- `deliveryCount: number` — number of uninvoiced deliveries
- `oldestDays: number` — age of oldest uninvoiced delivery
- `estimatedRevenue: string` — formatted EUR amount e.g. "€12,450.00"

Default export `UninvoicedAlertEmail`.

Preview: `${deliveryCount} uninvoiced deliveries — oldest: ${oldestDays} days`

Body content:
- Heading: "Uninvoiced Delivery Alert"
- Text: `${deliveryCount} deliveries remain uninvoiced. The oldest is ${oldestDays} days past delivery.`
- Section: Uninvoiced Count, Oldest Delivery Age, Estimated Revenue
- "View financial records" link to `/financial`

**3. `apps/web/emails/defective-batch-alert.tsx`:**

Props interface `DefectiveBatchAlertEmailProps`:
- `intakeReference: string`
- `facilityName: string`
- `batchNumber: string`
- `flagReason: string`
- `intakeId: string`

Default export `DefectiveBatchAlertEmail`.

Preview: `Defective batch match: ${batchNumber} at ${facilityName}`

Body content:
- Heading: "Defective Batch Match"
- Text: `Batch ${batchNumber} entered during intake ${intakeReference} at ${facilityName} matches a flagged batch entry.`
- Section: Reference, Facility, Batch Number, Flag Reason
- "View in platform" link to `/intake/${intakeId}`

**4. `apps/web/emails/facility-inactive-alert.tsx`:**

Props interface `FacilityInactiveAlertEmailProps`:
- `facilityName: string`
- `lastIntakeDate: string` — formatted date string
- `daysSinceLastIntake: number`

Default export `FacilityInactiveAlertEmail`.

Preview: `${facilityName} inactive — no intake for ${daysSinceLastIntake} days`

Body content:
- Heading: "Facility Inactive Alert"
- Text: `${facilityName} has not registered any intake for ${daysSinceLastIntake} days (last intake: ${lastIntakeDate}).`
- Section: Facility, Last Intake Date, Days Inactive
- "View settings" link to `/settings`
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/emails/discrepancy-alert.tsx contains `export default function DiscrepancyAlertEmail`
    - apps/web/emails/discrepancy-alert.tsx contains `from '@react-email/components'`
    - apps/web/emails/uninvoiced-alert.tsx contains `export default function UninvoicedAlertEmail`
    - apps/web/emails/defective-batch-alert.tsx contains `export default function DefectiveBatchAlertEmail`
    - apps/web/emails/facility-inactive-alert.tsx contains `export default function FacilityInactiveAlertEmail`
    - All four files contain `interface.*EmailProps`
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Four critical event email templates created, all using @react-email/components with typed props interfaces</done>
</task>

<task type="auto">
  <name>Task 2: Create non-critical event email templates (2 templates)</name>
  <files>
    apps/web/emails/warehouse-ageing-alert.tsx
    apps/web/emails/pickup-confirmed.tsx
  </files>
  <read_first>
    apps/web/emails/pickup-admin-alert.tsx
  </read_first>
  <action>
**1. `apps/web/emails/warehouse-ageing-alert.tsx`:**

Props interface `WarehouseAgeingAlertEmailProps`:
- `providerName: string` — consolidation provider name
- `warehouseAddress: string`
- `palletCount: number` — number of pallets exceeding threshold
- `oldestDays: number` — days held for oldest pallet
- `thresholdDays: number` — configured ageing threshold

Default export `WarehouseAgeingAlertEmail`.

Preview: `${palletCount} pallets at ${providerName} — oldest: ${oldestDays} days`

Body content:
- Heading: "Warehouse Ageing Alert"
- Text: `${palletCount} pallets at ${providerName} (${warehouseAddress}) have exceeded the ${thresholdDays}-day ageing threshold. The oldest pallet has been held for ${oldestDays} days.`
- Section: Provider, Warehouse, Pallets Over Threshold, Oldest, Threshold
- "View warehouse inventory" link to `/transport/outbound`

**2. `apps/web/emails/pickup-confirmed.tsx`:**

This is the client-facing confirmation email sent when reco-admin confirms a pickup.

Props interface `PickupConfirmedEmailProps`:
- `reference: string` — PU-YYYY-NNNN
- `clientName: string`
- `locationName: string`
- `confirmedDate: string` — the confirmed pickup date
- `pickupId: string`

Default export `PickupConfirmedEmail`.

Preview: `Pickup ${reference} confirmed for ${confirmedDate}`

Body content:
- Heading: "Pickup Confirmed"
- Text: `Your pickup request ${reference} has been confirmed.`
- Section: Reference, Location, Confirmed Date
- "View pickup" link to `/pickups/${pickupId}`

Both templates follow the exact same inline style pattern as pickup-admin-alert.tsx.
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/emails/warehouse-ageing-alert.tsx contains `export default function WarehouseAgeingAlertEmail`
    - apps/web/emails/warehouse-ageing-alert.tsx contains `WarehouseAgeingAlertEmailProps`
    - apps/web/emails/pickup-confirmed.tsx contains `export default function PickupConfirmedEmail`
    - apps/web/emails/pickup-confirmed.tsx contains `PickupConfirmedEmailProps`
    - Both files contain `from '@react-email/components'`
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Six total email templates complete, covering all gaps in the PRD notification matrix</done>
</task>

</tasks>

<verification>
- All six new email template files exist in apps/web/emails/
- Each exports a default function component with a typed props interface
- Each imports from '@react-email/components'
- TypeScript compiles without errors
</verification>

<success_criteria>
- Six email templates created matching the PRD notification matrix gaps
- All templates follow the established pickup-admin-alert.tsx pattern
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-notifications-and-manuals/09-02-SUMMARY.md`
</output>
