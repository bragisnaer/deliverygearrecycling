---
phase: 07-financial-tracking
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx
  - apps/web/app/(ops)/financial/components/currency-toggle.tsx
  - apps/web/app/(ops)/dashboard/page.tsx
autonomous: true
requirements:
  - FIN-04
  - FIN-05
must_haves:
  truths:
    - "Dashboard shows a persistent alert widget listing count of uninvoiced deliveries older than 14 days"
    - "Alert widget shows monthly uninvoiced revenue estimate in the user's chosen currency"
    - "User can toggle between EUR and DKK display via a currency toggle component"
    - "Currency toggle persists across page reloads via cookie"
    - "Exchange rate is read from system_settings and applied at display time only"
  artifacts:
    - path: "apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx"
      provides: "Uninvoiced delivery alert widget"
      min_lines: 30
    - path: "apps/web/app/(ops)/financial/components/currency-toggle.tsx"
      provides: "EUR/DKK currency toggle with cookie persistence"
      min_lines: 20
  key_links:
    - from: "apps/web/app/(ops)/dashboard/page.tsx"
      to: "apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx"
      via: "component import and render"
      pattern: "UninvoicedAlert"
    - from: "apps/web/app/(ops)/financial/components/currency-toggle.tsx"
      to: "cookie"
      via: "display_currency cookie set/read"
      pattern: "display_currency"
---

<objective>
Build the uninvoiced delivery alert widget for the dashboard and the EUR/DKK currency toggle with cookie persistence.

Purpose: Delivers the financial monitoring alert (FIN-04) that shows reco-admin overdue uninvoiced deliveries, and the currency display toggle (FIN-05) that applies the system exchange rate at display time only.
Output: UninvoicedAlert component rendered on ops dashboard, CurrencyToggle component, cookie-based preference.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-financial-tracking/07-RESEARCH.md
@.planning/phases/07-financial-tracking/07-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 -->

From apps/web/app/(ops)/financial/actions.ts:
```typescript
export async function getUninvoicedAlerts(thresholdDays?: number): Promise<{
  overdue_count: number;
  overdue_total_eur: string | null;
  monthly_uninvoiced_eur: string | null;
}>

export function formatCurrency(
  amountEur: string | null,
  currency: 'EUR' | 'DKK',
  exchangeRate: string
): string
```

From packages/db/src/schema/settings.ts:
```typescript
export const systemSettings = pgTable('system_settings', {
  exchange_rate_eur_dkk: numeric('exchange_rate_eur_dkk', { precision: 10, scale: 4 }).notNull().default('7.4600'),
  warehouse_ageing_threshold_days: integer('warehouse_ageing_threshold_days').notNull().default(14),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uninvoiced alert widget and currency toggle components</name>
  <files>apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx, apps/web/app/(ops)/financial/components/currency-toggle.tsx</files>
  <read_first>
    - apps/web/app/(ops)/financial/actions.ts (getUninvoicedAlerts signature, formatCurrency)
    - apps/web/app/(ops)/transport/outbound/page.tsx (existing alert/card UI pattern for reference)
    - apps/web/app/(ops)/settings/general-settings-form.tsx (existing form component pattern)
    - packages/db/src/schema/settings.ts (exchange_rate_eur_dkk field)
  </read_first>
  <action>
    1. Create `apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx` (Server Component):
       - Import `getUninvoicedAlerts` from `../actions`, `formatCurrency` from `../actions`
       - Import `db`, `systemSettings` from `@repo/db` for exchange rate
       - Import `cookies` from `next/headers` for reading display_currency
       - Props: none (self-contained async Server Component)
       - Implementation:
         - Read `display_currency` cookie (default 'EUR' if not set): `const cookieStore = await cookies(); const currency = (cookieStore.get('display_currency')?.value as 'EUR' | 'DKK') ?? 'EUR'`
         - Read system_settings for exchange_rate_eur_dkk (raw db, no RLS — settings are non-sensitive, same pattern as Phase 5): `const [settings] = await db.select().from(systemSettings).limit(1)`
         - Call `getUninvoicedAlerts()` (uses default 14-day threshold from system_settings internally)
         - If `overdue_count === 0`, render nothing (return null)
         - If overdue_count > 0, render a Card with:
           - Red/destructive border or background accent
           - Title: "Uninvoiced Deliveries"
           - Body: "{overdue_count} deliveries older than 14 days remain uninvoiced"
           - Total overdue amount: formatCurrency(overdue_total_eur, currency, exchangeRate)
           - Monthly estimate: "Uninvoiced this month: {formatCurrency(monthly_uninvoiced_eur, currency, exchangeRate)}"
           - Link to `/financial?status=not_invoiced` text: "View all uninvoiced"

    2. Create `apps/web/app/(ops)/financial/components/currency-toggle.tsx` (Client Component):
       - `'use client'`
       - Props: `{ currentCurrency: 'EUR' | 'DKK' }`
       - Render two buttons or a Select dropdown for EUR / DKK
       - On change, set cookie `display_currency` via a Server Action:

       Create a `setCurrencyPreference` Server Action in actions.ts (or inline):
       ```typescript
       export async function setCurrencyPreference(currency: 'EUR' | 'DKK') {
         const { cookies } = await import('next/headers')
         const cookieStore = await cookies()
         cookieStore.set('display_currency', currency, {
           path: '/',
           maxAge: 60 * 60 * 24 * 365, // 1 year
           sameSite: 'lax',
         })
       }
       ```

       - After setting cookie, call `router.refresh()` to re-render Server Components with new currency
       - Visually indicate current selection (bold/underline/active state)
       - Compact inline design — two small buttons: `EUR | DKK` with active state highlighted
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && test -f "apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx" && test -f "apps/web/app/(ops)/financial/components/currency-toggle.tsx" && grep -l "getUninvoicedAlerts" "apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx" && grep -l "display_currency" "apps/web/app/(ops)/financial/components/currency-toggle.tsx" && echo "COMPONENTS OK"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx exists
    - uninvoiced-alert.tsx does NOT have 'use client' (Server Component)
    - uninvoiced-alert.tsx calls `getUninvoicedAlerts()`
    - uninvoiced-alert.tsx calls `formatCurrency(` for amount display
    - uninvoiced-alert.tsx contains `overdue_count` check for conditional render
    - uninvoiced-alert.tsx contains link to `/financial` with status filter
    - apps/web/app/(ops)/financial/components/currency-toggle.tsx exists with `'use client'`
    - currency-toggle.tsx contains `display_currency` cookie name
    - currency-toggle.tsx contains `EUR` and `DKK` as selectable options
    - actions.ts (or separate file) contains `setCurrencyPreference` Server Action with cookie set
  </acceptance_criteria>
  <done>UninvoicedAlert Server Component renders red alert card with overdue count, total, and monthly estimate using display currency; CurrencyToggle client component switches EUR/DKK via cookie with router.refresh()</done>
</task>

<task type="auto">
  <name>Task 2: Wire uninvoiced alert into ops dashboard and currency toggle into financial pages</name>
  <files>apps/web/app/(ops)/dashboard/page.tsx, apps/web/app/(ops)/financial/page.tsx</files>
  <read_first>
    - apps/web/app/(ops)/dashboard/page.tsx (existing dashboard page — add alert widget)
    - apps/web/app/(ops)/financial/page.tsx (created in Plan 02 — add currency toggle)
    - apps/web/app/(ops)/financial/components/uninvoiced-alert.tsx (just created)
    - apps/web/app/(ops)/financial/components/currency-toggle.tsx (just created)
    - apps/web/app/(ops)/financial/actions.ts (formatCurrency, setCurrencyPreference)
  </read_first>
  <action>
    1. Update `apps/web/app/(ops)/dashboard/page.tsx`:
       - Import `UninvoicedAlert` from `../financial/components/uninvoiced-alert`
       - Import `auth` from `@/auth` (if not already imported)
       - Add role check: only render UninvoicedAlert if user.role is 'reco-admin' or (user.role is 'reco' and user.can_view_financials is true)
       - Render `<UninvoicedAlert />` at the top of the dashboard content (before any existing dashboard widgets/cards)
       - Wrap in a conditional: `{hasFinancialAccess && <UninvoicedAlert />}`
       - The UninvoicedAlert is an async Server Component so it renders inline in the Server Component dashboard page

    2. Update `apps/web/app/(ops)/financial/page.tsx`:
       - Import `CurrencyToggle` from `./components/currency-toggle`
       - Import `cookies` from `next/headers`
       - Read `display_currency` cookie at page level: `const cookieStore = await cookies(); const currency = (cookieStore.get('display_currency')?.value as 'EUR' | 'DKK') ?? 'EUR'`
       - Read exchange rate from system_settings (raw db): `const [settings] = await db.select().from(systemSettings).limit(1); const exchangeRate = settings?.exchange_rate_eur_dkk ?? '7.4600'`
       - Render `<CurrencyToggle currentCurrency={currency} />` in the page header area (next to page title)
       - Update all `formatCurrency` calls in the table to use the `currency` variable and `exchangeRate` instead of hardcoded 'EUR'
       - Transport Cost and Estimated Amount columns now display in the user's chosen currency
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && grep -l "UninvoicedAlert" "apps/web/app/(ops)/dashboard/page.tsx" && grep -l "CurrencyToggle" "apps/web/app/(ops)/financial/page.tsx" && grep "display_currency" "apps/web/app/(ops)/financial/page.tsx" && echo "WIRING OK"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(ops)/dashboard/page.tsx imports `UninvoicedAlert` from `../financial/components/uninvoiced-alert`
    - dashboard/page.tsx has conditional render: checks for reco-admin or reco+can_view_financials before rendering UninvoicedAlert
    - dashboard/page.tsx renders `<UninvoicedAlert />` (or `<UninvoicedAlert/>`)
    - apps/web/app/(ops)/financial/page.tsx imports `CurrencyToggle`
    - financial/page.tsx reads `display_currency` cookie
    - financial/page.tsx reads `exchange_rate_eur_dkk` from systemSettings
    - financial/page.tsx passes `currency` and `exchangeRate` to `formatCurrency` calls
    - financial/page.tsx renders `<CurrencyToggle currentCurrency={currency} />`
  </acceptance_criteria>
  <done>Ops dashboard renders UninvoicedAlert widget (role-gated) showing overdue deliveries at top of page; financial records list page renders CurrencyToggle and displays all amounts in user's preferred currency (EUR or DKK) using system exchange rate</done>
</task>

</tasks>

<verification>
- `grep 'UninvoicedAlert' apps/web/app/(ops)/dashboard/page.tsx` returns match
- `grep 'CurrencyToggle' apps/web/app/(ops)/financial/page.tsx` returns match
- `grep 'display_currency' apps/web/app/(ops)/financial/page.tsx` returns match
- `grep 'display_currency' apps/web/app/(ops)/financial/components/currency-toggle.tsx` returns match
- `pnpm test --run` — all tests pass (no regressions)
</verification>

<success_criteria>
- Uninvoiced alert widget appears on ops dashboard for reco-admin and reco+can_view_financials users
- Alert shows count of overdue deliveries (>14 days not_invoiced) with total amount
- Alert shows monthly uninvoiced revenue estimate
- Currency toggle on financial page switches between EUR and DKK display
- Toggle persists across page reloads via cookie
- Exchange rate is read from system_settings and applied at display time only (no stored DKK values)
</success_criteria>

<output>
After completion, create `.planning/phases/07-financial-tracking/07-03-SUMMARY.md`
</output>
