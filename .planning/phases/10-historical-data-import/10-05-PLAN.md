---
phase: 10-historical-data-import
plan: 05
type: execute
wave: 3
depends_on:
  - 10-03
  - 10-04
files_modified: []
autonomous: false
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03
  - IMPORT-04
must_haves:
  truths:
    - "Full import flow works end-to-end for at least one source"
    - "Validation errors are shown before commit"
    - "Imported records display badge in list views"
    - "Re-commit of same job is prevented"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete import flow end-to-end: upload, map columns, preview validation, commit, and confirm imported records appear with badges in list views.

Purpose: Final verification that all four IMPORT requirements are satisfied together as a working feature.

Output: Confirmed working import system ready for production use.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-historical-data-import/10-03-SUMMARY.md
@.planning/phases/10-historical-data-import/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and type check</name>
  <files></files>
  <read_first>
    apps/web/lib/import-parser.test.ts,
    apps/web/lib/import-validators.test.ts
  </read_first>
  <action>
1. Run the full test suite:
   ```bash
   pnpm --filter @apps/web vitest run
   ```
2. Run TypeScript type check:
   ```bash
   pnpm --filter @apps/web tsc --noEmit
   ```
3. Fix any test failures or type errors introduced by Phase 10 changes.
4. Verify no regressions in existing tests — particularly:
   - Intake-related tests (is_imported column added to schema)
   - Pickup-related tests (is_imported column added to schema)
   - Financial-related tests (is_imported column added to schema)
  </action>
  <verify>
    <automated>pnpm --filter @apps/web vitest run && pnpm --filter @apps/web tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @apps/web vitest run` exits 0 with all tests passing
    - `pnpm --filter @apps/web tsc --noEmit` exits 0 with no type errors
    - import-parser tests pass
    - import-validators tests pass
    - No regressions in existing test files
  </acceptance_criteria>
  <done>Full test suite green. No type errors. No regressions from Phase 10 schema changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification of complete import flow</name>
  <files></files>
  <action>Human verifies the complete import flow end-to-end by uploading a test CSV, mapping columns, previewing validation, committing, and confirming badges appear.</action>
  <what-built>Complete historical data import system: file upload API, multi-step wizard UI (upload, column mapping, validation preview, commit), five import source definitions, imported record badges in all list views, re-commit prevention.</what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev`
    2. Log in as reco-admin
    3. Navigate to the Import page via ops nav bar — verify 5 source cards are visible
    4. Click one source (e.g. "Prison Delivery Intake Log")
    5. Select a tenant from the dropdown
    6. Upload a small test CSV with a few rows (create one with columns: facility_name, staff_name, delivery_date, origin_market)
    7. Verify the column mapping step shows spreadsheet headers with dropdown selectors for platform fields
    8. Map columns and click "Preview Validation"
    9. Verify validation errors are shown with row numbers and field names
    10. If valid rows exist, click "Commit Import"
    11. Navigate to the intake list view and verify imported records show the blue "Imported" badge
    12. Try to re-commit the same job — verify it is rejected
  </how-to-verify>
  <verify>Human confirms all steps pass</verify>
  <done>All four IMPORT requirements verified working end-to-end.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full test suite passes
- Type check passes
- Manual verification of import flow confirms all four requirements
</verification>

<success_criteria>
- IMPORT-01: CSV/XLSX upload and parsing works for all five sources
- IMPORT-02: Column mapping UI, validation preview with errors shown before commit, one-click commit
- IMPORT-03: Imported records show blue "Imported" badge in all list views; ESG and financial aggregates include imported data
- IMPORT-04: Committed jobs cannot be re-committed
</success_criteria>

<output>
After completion, create `.planning/phases/10-historical-data-import/10-05-SUMMARY.md`
</output>
