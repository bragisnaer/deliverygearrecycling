---
phase: 10-historical-data-import
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/web/app/(ops)/intake/components/intake-queue-table.tsx
  - apps/web/app/(ops)/pickups/components/pickup-queue-table.tsx
  - apps/web/app/(ops)/processing/page.tsx
  - apps/web/app/(ops)/financial/page.tsx
  - apps/web/app/(ops)/transport/page.tsx
  - apps/web/app/(ops)/esg/page.tsx
autonomous: true
requirements:
  - IMPORT-03
must_haves:
  truths:
    - "Imported records display a visible 'Imported' badge in all list views"
    - "The badge is visually distinct from other status badges (uses blue colour scheme)"
    - "ESG and financial summaries include imported records without filtering them out"
    - "ESG CSV and PDF exports include imported records (no is_imported=false filter in export queries or serialization)"
  artifacts:
    - path: "apps/web/app/(ops)/intake/components/intake-queue-table.tsx"
      provides: "Imported badge on intake list"
      contains: "is_imported"
    - path: "apps/web/app/(ops)/pickups/components/pickup-queue-table.tsx"
      provides: "Imported badge on pickup list"
      contains: "is_imported"
  key_links:
    - from: "apps/web/app/(ops)/intake/components/intake-queue-table.tsx"
      to: "packages/db/src/schema/intake.ts"
      via: "is_imported column query"
      pattern: "is_imported"
    - from: "apps/web/app/(ops)/esg/export/route.ts"
      to: "apps/web/app/(ops)/esg/actions.ts"
      via: "getEsgData aggregation includes all records"
      pattern: "getEsgData"
---

<objective>
Add the "Imported" source badge to all list views where imported records appear, and verify that ESG/financial exports include imported data, ensuring IMPORT-03 is fully satisfied.

Purpose: IMPORT-03 requires imported records to be "distinguishable from live data in all views and exports." This plan adds a blue "Imported" badge (matching the existing is_unexpected amber badge pattern) to intake, pickup, processing, financial, and transport list views. It also verifies ESG/financial queries and export paths do not inadvertently filter out is_imported records.

Output: Imported badge visible in all 5 list views. ESG and financial aggregates and exports confirmed to include imported data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-historical-data-import/10-RESEARCH.md
@.planning/phases/10-historical-data-import/10-01-SUMMARY.md

@apps/web/app/(ops)/intake/components/intake-queue-table.tsx
@apps/web/app/(ops)/pickups/components/pickup-queue-table.tsx
@apps/web/app/(ops)/esg/export/route.ts
@apps/web/app/(ops)/esg/actions.ts
@apps/web/lib/esg-calculator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Imported badge to intake and pickup list views</name>
  <files>
    apps/web/app/(ops)/intake/components/intake-queue-table.tsx,
    apps/web/app/(ops)/pickups/components/pickup-queue-table.tsx,
    apps/web/app/(ops)/intake/actions.ts,
    apps/web/app/(ops)/pickups/actions.ts
  </files>
  <read_first>
    apps/web/app/(ops)/intake/components/intake-queue-table.tsx,
    apps/web/app/(ops)/pickups/components/pickup-queue-table.tsx,
    apps/web/app/(ops)/intake/actions.ts,
    apps/web/app/(ops)/pickups/actions.ts
  </read_first>
  <action>
1. Update intake queue data fetching:
   - In the action/query that fetches intake records for the queue table, add `is_imported` to the SELECT columns (e.g. `intakeRecords.is_imported`). Find the query function (likely `getIntakeQueue` or similar in `apps/web/app/(ops)/intake/actions.ts`) and ensure it selects `is_imported`.

2. Update `apps/web/app/(ops)/intake/components/intake-queue-table.tsx`:
   - In the table row rendering, after the existing status/flag badges (quarantine, discrepancy, unexpected), add:
     ```tsx
     {record.is_imported && (
       <span className="inline-flex items-center rounded-full border px-2.5 py-0.5 font-mono text-[12px] font-medium bg-blue-100 text-blue-800 border-blue-200">
         Imported
       </span>
     )}
     ```
   - The blue colour scheme (bg-blue-100, text-blue-800, border-blue-200) is distinct from the amber quarantine badge and the existing status colours.

3. Update pickup queue data fetching:
   - In the action that fetches pickups for the queue table (likely in `apps/web/app/(ops)/pickups/actions.ts`), add `is_imported` to SELECT columns.

4. Update `apps/web/app/(ops)/pickups/components/pickup-queue-table.tsx`:
   - Add the same blue "Imported" badge after existing status indicators:
     ```tsx
     {pickup.is_imported && (
       <span className="inline-flex items-center rounded-full border px-2.5 py-0.5 font-mono text-[12px] font-medium bg-blue-100 text-blue-800 border-blue-200">
         Imported
       </span>
     )}
     ```
  </action>
  <verify>
    <automated>grep -c "is_imported" apps/web/app/\(ops\)/intake/components/intake-queue-table.tsx apps/web/app/\(ops\)/pickups/components/pickup-queue-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(ops)/intake/components/intake-queue-table.tsx contains `is_imported` and renders "Imported" badge with bg-blue-100
    - apps/web/app/(ops)/pickups/components/pickup-queue-table.tsx contains `is_imported` and renders "Imported" badge with bg-blue-100
    - Intake queue query selects is_imported column
    - Pickup queue query selects is_imported column
    - `pnpm --filter @apps/web tsc --noEmit` passes
  </acceptance_criteria>
  <done>Intake and pickup list views show blue "Imported" badge on imported records.</done>
</task>

<task type="auto">
  <name>Task 2: Add Imported badge to processing, financial, transport views + verify ESG exports include imported records</name>
  <files>
    apps/web/app/(ops)/processing/page.tsx,
    apps/web/app/(ops)/financial/page.tsx,
    apps/web/app/(ops)/financial/actions.ts,
    apps/web/app/(ops)/transport/page.tsx
  </files>
  <read_first>
    apps/web/app/(ops)/processing/page.tsx,
    apps/web/app/(ops)/financial/page.tsx,
    apps/web/app/(ops)/financial/actions.ts,
    apps/web/app/(ops)/transport/page.tsx,
    apps/web/app/(ops)/esg/page.tsx,
    apps/web/app/(ops)/esg/actions.ts,
    apps/web/app/(ops)/esg/export/route.ts,
    apps/web/lib/esg-calculator.ts,
    apps/web/app/(ops)/dashboard/actions.ts
  </read_first>
  <action>
1. Update processing list view:
   - Find the processing reports list rendering in `apps/web/app/(ops)/processing/page.tsx` (or its sub-components)
   - Add `is_imported` to the data query for processing reports
   - Add the blue "Imported" badge in the list/card rendering:
     ```tsx
     {report.is_imported && (
       <span className="inline-flex items-center rounded-full border px-2.5 py-0.5 font-mono text-[12px] font-medium bg-blue-100 text-blue-800 border-blue-200">
         Imported
       </span>
     )}
     ```

2. Update financial records list view:
   - In `apps/web/app/(ops)/financial/actions.ts`, find the query that fetches financial records for the list page and add `is_imported` to SELECT. financial_records has is_imported per our migration — select it directly.
   - In `apps/web/app/(ops)/financial/page.tsx` (or its list component), add the blue "Imported" badge.

3. Update transport bookings list view:
   - In the transport list data query, add `is_imported` to SELECT
   - In `apps/web/app/(ops)/transport/page.tsx` (or its list component), add the blue "Imported" badge.

4. Verify ESG export inclusion (CRITICAL for IMPORT-03 "all views and exports"):
   - Read `apps/web/app/(ops)/esg/actions.ts` — specifically `getEsgData` and `getProcessingStreamCounts`
   - Confirm these queries do NOT have `WHERE is_imported = false` or equivalent filter
   - Read `apps/web/app/(ops)/esg/export/route.ts` — confirm the CSV and PDF export paths call `getEsgData` without filtering
   - Read `apps/web/lib/esg-calculator.ts` — confirm `serializeEsgCsv` and other functions do not filter by is_imported
   - If any query or function filters out imported records, REMOVE that filter
   - No code change expected here unless a filter is found — document the verification result in commit message

5. Verify dashboard inclusion:
   - Read `apps/web/app/(ops)/dashboard/actions.ts`
   - Confirm dashboard aggregate queries do not exclude is_imported records
   - No code change expected — just verification
  </action>
  <verify>
    <automated>grep -c "is_imported" apps/web/app/\(ops\)/processing/page.tsx apps/web/app/\(ops\)/financial/page.tsx apps/web/app/\(ops\)/transport/page.tsx 2>/dev/null; echo "---"; grep -rn "is_imported.*false\|is_imported.*=.*false" apps/web/app/\(ops\)/esg/ apps/web/app/\(ops\)/dashboard/ apps/web/lib/esg-calculator.ts 2>/dev/null || echo "No is_imported exclusion filters in ESG/dashboard/export paths (correct)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(ops)/processing/page.tsx (or sub-component) contains "Imported" badge with bg-blue-100
    - apps/web/app/(ops)/financial/page.tsx (or sub-component) contains "Imported" badge with bg-blue-100
    - apps/web/app/(ops)/transport/page.tsx (or sub-component) contains "Imported" badge with bg-blue-100
    - ESG queries in esg/actions.ts do NOT contain `is_imported = false` filter
    - ESG export route (esg/export/route.ts) does NOT filter out imported records
    - serializeEsgCsv in esg-calculator.ts does NOT filter by is_imported
    - Dashboard queries in dashboard/actions.ts do NOT contain `is_imported = false` filter
    - `pnpm --filter @apps/web tsc --noEmit` passes
  </acceptance_criteria>
  <done>All five list views show blue "Imported" badge. ESG exports (CSV and PDF) and dashboard aggregates confirmed to include imported records without filtering.</done>
</task>

</tasks>

<verification>
- `grep -r "Imported" apps/web/app/\(ops\)/intake/ apps/web/app/\(ops\)/pickups/ apps/web/app/\(ops\)/processing/ apps/web/app/\(ops\)/financial/ apps/web/app/\(ops\)/transport/ | grep "bg-blue-100" | wc -l` returns >= 5
- `grep -rn "is_imported.*false" apps/web/app/\(ops\)/esg/ apps/web/app/\(ops\)/dashboard/ apps/web/lib/esg-calculator.ts | wc -l` returns 0
- `pnpm --filter @apps/web tsc --noEmit` passes
</verification>

<success_criteria>
- Imported records are visually distinguishable in all five list views (intake, pickup, processing, financial, transport)
- Badge uses blue colour scheme (bg-blue-100, text-blue-800, border-blue-200) — distinct from other badges
- ESG totals and financial summaries include imported data (no exclusion filter)
- ESG CSV and PDF exports include imported records (verified no is_imported filter in export paths)
</success_criteria>

<output>
After completion, create `.planning/phases/10-historical-data-import/10-04-SUMMARY.md`
</output>
