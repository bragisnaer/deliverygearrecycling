---
phase: 06-prison-processing-dispatch-and-audit-trail
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/processing.ts
  - packages/db/src/schema/products.ts
  - packages/db/src/schema/index.ts
autonomous: true
requirements:
  - PROCESS-02

must_haves:
  truths:
    - "processing_reports table exists with activity_type enum (wash/pack), product_id FK, staff_name, report_date"
    - "processing_report_lines table exists with nullable size_bucket enum and quantity"
    - "product_category enum column exists on products table to distinguish clothing from bags"
    - "size_bucket enum defined once in processing.ts and reusable by dispatch schema"
  artifacts:
    - path: "packages/db/src/schema/processing.ts"
      provides: "processing_reports and processing_report_lines tables, activity_type and size_bucket enums"
      exports: ["processingReports", "processingReportLines", "activityTypeEnum", "sizeBucketEnum"]
    - path: "packages/db/src/schema/products.ts"
      provides: "product_category enum column on products table"
      contains: "product_category"
  key_links:
    - from: "packages/db/src/schema/processing.ts"
      to: "packages/db/src/schema/intake.ts"
      via: "intake_record_id FK on processing_reports"
      pattern: "references.*intakeRecords"
    - from: "packages/db/src/schema/processing.ts"
      to: "packages/db/src/schema/products.ts"
      via: "product_id FK on processing_reports"
      pattern: "references.*products"
---

<objective>
Create the Drizzle schema for processing reports (wash and pack records) with size bucket support for clothing, and add product_category enum to the products table. This is the schema-only plan — migration SQL will be written in Plan 01's migration file or a companion file.

Purpose: PROCESS-02 requires processing report fields with size-bucket quantities for clothing and total quantity for bags. The schema must exist before any Server Actions or UI can be built.
Output: Drizzle schema files with RLS policies, enums, and FK relationships.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@packages/db/src/schema/intake.ts
@packages/db/src/schema/products.ts
@packages/db/src/schema/auth.ts
@packages/db/src/schema/index.ts
</context>

<interfaces>
<!-- Key types from existing schema that processing.ts must reference -->

From packages/db/src/schema/auth.ts:
```typescript
export const recoAdminRole = pgRole('reco_admin_role')
export const recoRole = pgRole('reco_role')
export const prisonRole = pgRole('prison_role')
export const users = pgTable('users', { id: uuid('id').primaryKey(), ... })
```

From packages/db/src/schema/intake.ts:
```typescript
export const intakeRecords = pgTable('intake_records', {
  id: uuid('id').primaryKey().defaultRandom(),
  prison_facility_id: uuid('prison_facility_id'),
  tenant_id: text('tenant_id'),
  ...
})
```

From packages/db/src/schema/products.ts:
```typescript
export const processingStreamEnum = pgEnum('processing_stream', ['recycling', 'reuse'])
export const products = pgTable('products', {
  id: uuid('id').primaryKey().defaultRandom(),
  processing_stream: processingStreamEnum('processing_stream').notNull(),
  ...
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Processing reports Drizzle schema with RLS policies</name>
  <files>packages/db/src/schema/processing.ts, packages/db/src/schema/index.ts</files>
  <read_first>
    - packages/db/src/schema/intake.ts (full file — copy RLS policy pattern exactly: deny-all restrictive + permissive per role)
    - packages/db/src/schema/auth.ts (role imports: recoAdminRole, recoRole, prisonRole, users)
    - packages/db/src/schema/products.ts (products table reference for FK)
    - packages/db/src/schema/tenants.ts (prisonFacilities, tenants references)
    - packages/db/src/schema/index.ts (add export line)
  </read_first>
  <action>
    Create `packages/db/src/schema/processing.ts` with the following exact definitions:

    1. `activityTypeEnum = pgEnum('activity_type', ['wash', 'pack'])`

    2. `sizeBucketEnum = pgEnum('size_bucket', ['XXS', 'XS', 'S', 'M', 'L', 'XL', 'XXL', 'XXXL'])`

    3. `processingReports = pgTable('processing_reports', {...})` with columns:
       - `id: uuid('id').primaryKey().defaultRandom()`
       - `prison_facility_id: uuid('prison_facility_id').notNull().references(() => prisonFacilities.id)`
       - `intake_record_id: uuid('intake_record_id').references(() => intakeRecords.id)` (nullable — processing can be unlinked)
       - `tenant_id: text('tenant_id').notNull().references(() => tenants.id)`
       - `staff_name: text('staff_name').notNull()`
       - `activity_type: activityTypeEnum('activity_type').notNull()`
       - `product_id: uuid('product_id').notNull().references(() => products.id)`
       - `report_date: timestamp('report_date').notNull()`
       - `notes: text('notes')`
       - `voided: boolean('voided').notNull().default(false)`
       - `void_reason: text('void_reason')`
       - `submitted_by: uuid('submitted_by').references(() => users.id)`
       - `created_at: timestamp('created_at').notNull().defaultNow()`
       - `updated_at: timestamp('updated_at').notNull().defaultNow()`

       Indexes: `tenant_id`, `prison_facility_id`, `intake_record_id`

       RLS policies (identical pattern to intake.ts):
       - `processing_reports_deny_all`: restrictive, for all, using: `false`
       - `processing_reports_prison_select`: permissive, to prisonRole, for select, using: `prison_facility_id::text = current_setting('request.jwt.claim.facility_id', true)`
       - `processing_reports_prison_insert`: permissive, to prisonRole, for insert, withCheck: same
       - `processing_reports_prison_update`: permissive, to prisonRole, for update, using+withCheck: same
       - `processing_reports_reco_select`: permissive, to recoRole, for select, using: `true`
       - `processing_reports_reco_admin_all`: permissive, to recoAdminRole, for all, using: `true`, withCheck: `true`

    4. `processingReportLines = pgTable('processing_report_lines', {...})` with columns:
       - `id: uuid('id').primaryKey().defaultRandom()`
       - `processing_report_id: uuid('processing_report_id').notNull().references(() => processingReports.id, { onDelete: 'cascade' })`
       - `size_bucket: sizeBucketEnum('size_bucket')` (nullable — null means bag/non-clothing total)
       - `quantity: integer('quantity').notNull()`
       - `created_at: timestamp('created_at').notNull().defaultNow()`

       Index: `processing_report_id`

       RLS policies: same deny-all + prison select + reco-admin all pattern. Prison INSERT via cascade from parent. Use EXISTS subquery on parent processing_reports for prison select:
       `EXISTS (SELECT 1 FROM processing_reports WHERE processing_reports.id = processing_report_lines.processing_report_id AND processing_reports.prison_facility_id::text = current_setting('request.jwt.claim.facility_id', true))`

    Update `packages/db/src/schema/index.ts` — add `export * from './processing'` after the intake line.
  </action>
  <verify>
    <automated>grep -q "processingReports" packages/db/src/schema/processing.ts && grep -q "sizeBucketEnum" packages/db/src/schema/processing.ts && grep -q "processing" packages/db/src/schema/index.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/db/src/schema/processing.ts` exists
    - Contains `activityTypeEnum` with values `['wash', 'pack']`
    - Contains `sizeBucketEnum` with values `['XXS', 'XS', 'S', 'M', 'L', 'XL', 'XXL', 'XXXL']`
    - Contains `processingReports` table with all specified columns
    - Contains `processingReportLines` table with nullable `size_bucket` column
    - Contains deny-all restrictive + permissive per-role RLS policies
    - Prison role has SELECT, INSERT, and UPDATE policies on processing_reports
    - `packages/db/src/schema/index.ts` contains `export * from './processing'`
  </acceptance_criteria>
  <done>Processing report Drizzle schema with full RLS and size-bucket support ready for migration</done>
</task>

<task type="auto">
  <name>Task 2: Add product_category enum to products table</name>
  <files>packages/db/src/schema/products.ts</files>
  <read_first>
    - packages/db/src/schema/products.ts (full file — understand current column layout before adding)
  </read_first>
  <action>
    Update `packages/db/src/schema/products.ts`:

    1. Add enum definition before the products table:
       ```typescript
       export const productCategoryEnum = pgEnum('product_category', ['clothing', 'bag', 'equipment', 'other'])
       ```

    2. Add column to `products` table after `processing_stream`:
       ```typescript
       product_category: productCategoryEnum('product_category').notNull().default('other'),
       ```

    This column will be used by the processing form to determine whether to show size-bucket inputs (clothing) or single total quantity (bag/equipment/other). The migration SQL in Plan 01's migration file will include the ALTER TABLE and seed UPDATE for existing Wolt products:
    - Clothing -> 'clothing'
    - Bike Bag, Car Bag, Inner Bag -> 'bag'
    - Heating Plate -> 'equipment'

    Note: The actual ALTER TABLE + UPDATE SQL goes in the migration file (0005). Add a comment in the schema file referencing this.
  </action>
  <verify>
    <automated>grep -q "productCategoryEnum" packages/db/src/schema/products.ts && grep -q "product_category" packages/db/src/schema/products.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/src/schema/products.ts` contains `productCategoryEnum` with values `['clothing', 'bag', 'equipment', 'other']`
    - Products table has `product_category` column with default `'other'`
    - Column is positioned after `processing_stream`
  </acceptance_criteria>
  <done>product_category column on products table enables clothing vs. bag detection for processing forms</done>
</task>

</tasks>

<verification>
- `packages/db/src/schema/processing.ts` compiles without TypeScript errors
- All FK references resolve to existing tables
- RLS policies follow deny-all + permissive pattern from intake.ts
- `packages/db/src/schema/index.ts` re-exports processing module
- products.ts has product_category enum column
</verification>

<success_criteria>
- Processing report schema defined with all required fields from PROCESS-02
- Size bucket enum supports XXS-XXXL for clothing
- Product category discriminator ready for form logic
- Schema exports available via @repo/db
</success_criteria>

<output>
After completion, create `.planning/phases/06-prison-processing-dispatch-and-audit-trail/06-04-SUMMARY.md`
</output>
