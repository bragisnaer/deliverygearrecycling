---
phase: 06-prison-processing-dispatch-and-audit-trail
plan: 08
type: execute
wave: 3
depends_on: ["06-07", "06-05"]
files_modified:
  - apps/web/app/prison/dispatch/page.tsx
  - apps/web/app/prison/actions.ts
  - apps/web/app/(ops)/dispatch/page.tsx
  - apps/web/app/(ops)/dispatch/new/page.tsx
  - apps/web/app/(ops)/dispatch/[id]/page.tsx
  - apps/web/app/(ops)/dispatch/components/dispatch-form.tsx
  - apps/web/app/(ops)/ops-nav-bar.tsx
  - messages/da.json
autonomous: true
requirements:
  - DISPATCH-04
  - DISPATCH-01
  - DISPATCH-02

must_haves:
  truths:
    - "Prison staff can view outbound dispatch history for their own facility as read-only"
    - "Prison staff cannot create dispatches — no create button visible"
    - "reco-admin can create dispatch records with packing list lines"
    - "Dispatch detail page shows packing list with product, size, SKU, quantity"
  artifacts:
    - path: "apps/web/app/prison/dispatch/page.tsx"
      provides: "Prison dispatch history view (read-only)"
    - path: "apps/web/app/(ops)/dispatch/page.tsx"
      provides: "Ops dispatch list page"
    - path: "apps/web/app/(ops)/dispatch/new/page.tsx"
      provides: "Create dispatch form page"
    - path: "apps/web/app/(ops)/dispatch/[id]/page.tsx"
      provides: "Dispatch detail with packing list and status actions"
  key_links:
    - from: "apps/web/app/prison/dispatch/page.tsx"
      to: "apps/web/app/prison/actions.ts"
      via: "calls getDispatchHistory"
      pattern: "getDispatchHistory"
    - from: "apps/web/app/(ops)/dispatch/new/page.tsx"
      to: "apps/web/app/(ops)/dispatch/actions.ts"
      via: "calls createDispatch"
      pattern: "createDispatch"
---

<objective>
Build the dispatch UI for both prison staff (read-only history view) and reco-admin (full CRUD: list, create with packing list, detail with status updates). Prison staff see dispatch history for own facility but cannot create.

Purpose: DISPATCH-04 requires prison staff read-only dispatch access. DISPATCH-01/02 require reco-admin dispatch creation with packing lists. This plan builds all dispatch UI.
Output: Prison dispatch view, ops dispatch list/create/detail pages, dispatch form.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-prison-processing-dispatch-and-audit-trail/06-07-SUMMARY.md
@apps/web/app/(ops)/dispatch/actions.ts
@packages/db/src/schema/dispatch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prison dispatch history view (read-only)</name>
  <files>apps/web/app/prison/dispatch/page.tsx, apps/web/app/prison/actions.ts, apps/web/app/prison/page.tsx, messages/da.json</files>
  <read_first>
    - apps/web/app/prison/page.tsx (add dispatch tab or link to prison home)
    - apps/web/app/prison/actions.ts (full file — add getDispatchHistory action)
    - apps/web/app/prison/incoming/page.tsx (if exists — reference page pattern for prison Server Components)
    - packages/db/src/schema/dispatch.ts (outboundDispatches, outboundDispatchLines tables)
  </read_first>
  <action>
    Extend `apps/web/app/prison/actions.ts` with:
    ```typescript
    export async function getDispatchHistory() {
      const user = await requirePrisonSession()
      // withRLSContext — prison_role SELECT policy on outbound_dispatches
      // filters to own facility automatically
      // JOIN outbound_dispatch_lines for line counts
      // ORDER BY dispatch_date DESC
      // Filter: voided = false
    }
    ```

    Create `apps/web/app/prison/dispatch/page.tsx` as Server Component:
    - Calls `getDispatchHistory()`
    - Renders a list/table of dispatches: dispatch date, destination, carrier, status badge, line count
    - NO create button (DISPATCH-04: view only)
    - Empty state: "Ingen forsendelser" (No dispatches)
    - Each row links to a read-only detail view showing packing list
    - Large touch targets, Danish labels via useTranslations

    Update `apps/web/app/prison/page.tsx`:
    - Add a third navigation option (Link or Tab) for dispatch history: `/prison/dispatch`
    - Label: "Forsendelseshistorik" (Dispatch history)

    Update `messages/da.json` with `dispatch` namespace:
    ```json
    "dispatch": {
      "title": "Forsendelser",
      "history": "Forsendelseshistorik",
      "date": "Forsendelsesdato",
      "destination": "Destination",
      "carrier": "Transportfirma",
      "status": "Status",
      "lines": "Linjer",
      "no_dispatches": "Ingen forsendelser",
      "created": "Oprettet",
      "picked_up": "Afhentet",
      "delivered": "Leveret",
      "packing_list": "Pakkeliste",
      "product": "Produkt",
      "size": "Str.",
      "sku": "SKU-kode",
      "quantity": "Antal",
      "create": "Opret forsendelse",
      "notes": "Noter"
    }
    ```
  </action>
  <verify>
    <automated>test -f apps/web/app/prison/dispatch/page.tsx && grep -q "getDispatchHistory" apps/web/app/prison/actions.ts && grep -q "dispatch" apps/web/app/prison/page.tsx && grep -q "dispatch.title" messages/da.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/prison/dispatch/page.tsx` exists
    - Page does NOT contain any create/new button or link
    - `getDispatchHistory` uses `withRLSContext` (RLS auto-filters to facility)
    - `getDispatchHistory` filters `voided = false`
    - Prison home page has navigation to `/prison/dispatch`
    - `messages/da.json` contains `dispatch.title`, `dispatch.history`, `dispatch.no_dispatches`
  </acceptance_criteria>
  <done>Prison staff can view dispatch history for own facility as read-only</done>
</task>

<task type="auto">
  <name>Task 2: Ops dispatch pages — list, create form, detail with packing list</name>
  <files>apps/web/app/(ops)/dispatch/page.tsx, apps/web/app/(ops)/dispatch/new/page.tsx, apps/web/app/(ops)/dispatch/[id]/page.tsx, apps/web/app/(ops)/dispatch/components/dispatch-form.tsx, apps/web/app/(ops)/ops-nav-bar.tsx</files>
  <read_first>
    - apps/web/app/(ops)/dispatch/actions.ts (createDispatch, updateDispatchStatus, getDispatches — from Plan 07)
    - apps/web/app/(ops)/intake/page.tsx (ops list page pattern — Table with status tabs)
    - apps/web/app/(ops)/intake/[id]/page.tsx (ops detail page pattern)
    - apps/web/app/(ops)/transport/page.tsx (if exists — similar list/create pattern)
    - apps/web/app/(ops)/ops-nav-bar.tsx (add Dispatch nav link)
    - packages/db/src/schema/dispatch.ts (tables and enums)
  </read_first>
  <action>
    Create `apps/web/app/(ops)/dispatch/page.tsx`:
    - Server Component calling `getDispatches()`
    - Table with columns: Date, Facility, Destination, Carrier, Status (badge), Lines
    - "Create Dispatch" button linking to `/dispatch/new`
    - Status badge colours: created=yellow, picked_up=blue, delivered=green
    - Each row links to `/dispatch/[id]`

    Create `apps/web/app/(ops)/dispatch/components/dispatch-form.tsx`:
    - Client Component with `'use client'` directive
    - Fields: prison facility (dropdown), tenant/client (dropdown), dispatch date, destination, carrier, notes
    - Packing list section: dynamic rows with product dropdown, size bucket (optional), SKU code (optional text), quantity
    - Add/remove line buttons
    - On submit: call createDispatch Server Action
    - On success: redirect to dispatch list with toast

    Create `apps/web/app/(ops)/dispatch/new/page.tsx`:
    - Server Component wrapper fetching facilities, clients, products
    - Passes data to DispatchForm

    Create `apps/web/app/(ops)/dispatch/[id]/page.tsx`:
    - Server Component showing dispatch detail + packing list table
    - Status update buttons (reco-admin): Mark Picked Up, Mark Delivered (using updateDispatchStatus)
    - Void button (using VoidRecordDialog from Plan 03)
    - EditedIndicator (from Plan 02) if record has been modified
    - Packing list table: Product, Size, SKU Code, Quantity

    Update `apps/web/app/(ops)/ops-nav-bar.tsx`:
    - Add "Dispatch" / "Forsendelse" nav link to `/dispatch`
  </action>
  <verify>
    <automated>test -f apps/web/app/\(ops\)/dispatch/page.tsx && test -f apps/web/app/\(ops\)/dispatch/new/page.tsx && test -f apps/web/app/\(ops\)/dispatch/\[id\]/page.tsx && grep -q "Dispatch\|Forsendelse\|dispatch" apps/web/app/\(ops\)/ops-nav-bar.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(ops)/dispatch/page.tsx` exists with dispatch list table
    - `apps/web/app/(ops)/dispatch/new/page.tsx` exists with DispatchForm
    - `apps/web/app/(ops)/dispatch/[id]/page.tsx` exists with detail + packing list + status buttons
    - DispatchForm has dynamic packing list rows with add/remove
    - Detail page includes VoidRecordDialog and EditedIndicator
    - Ops nav bar has Dispatch link at `/dispatch`
  </acceptance_criteria>
  <done>Full ops dispatch CRUD UI: list, create with packing list, detail with status lifecycle</done>
</task>

</tasks>

<verification>
- Prison dispatch view shows dispatches but no create button
- Ops dispatch list shows all dispatches with status badges
- Create form builds packing list with product/size/SKU/quantity lines
- Detail page shows packing list and status update buttons
- Ops nav has Dispatch link
</verification>

<success_criteria>
- Prison staff view dispatch history (read-only, own facility)
- reco-admin creates dispatches with packing lists
- Dispatch detail shows full packing list
- Status lifecycle buttons work
</success_criteria>

<output>
After completion, create `.planning/phases/06-prison-processing-dispatch-and-audit-trail/06-08-SUMMARY.md`
</output>
