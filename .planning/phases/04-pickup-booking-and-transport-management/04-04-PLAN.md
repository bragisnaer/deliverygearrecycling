---
phase: 04-pickup-booking-and-transport-management
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/web/app/(ops)/transport/providers/page.tsx
  - apps/web/app/(ops)/transport/providers/new/page.tsx
  - apps/web/app/(ops)/transport/providers/[id]/page.tsx
  - apps/web/app/(ops)/transport/providers/actions.ts
autonomous: true
requirements:
  - TRANS-01
  - TRANS-02

must_haves:
  truths:
    - "reco-admin can list, create, and edit transport providers"
    - "Transport providers have type (direct/consolidation), contact details, and service regions"
    - "Providers are linked to specific tenants via a join table"
  artifacts:
    - path: "apps/web/app/(ops)/transport/providers/page.tsx"
      provides: "Transport provider list page"
      min_lines: 40
    - path: "apps/web/app/(ops)/transport/providers/new/page.tsx"
      provides: "Create transport provider form"
      min_lines: 40
    - path: "apps/web/app/(ops)/transport/providers/actions.ts"
      provides: "CRUD Server Actions for providers"
      exports: ["createTransportProvider", "updateTransportProvider"]
  key_links:
    - from: "apps/web/app/(ops)/transport/providers/actions.ts"
      to: "packages/db/src/schema/transport.ts"
      via: "withRLSContext insert/update transportProviders and transportProviderClients"
      pattern: "transportProviders|transportProviderClients"
---

<objective>
Build the transport provider registry CRUD: list, create, edit. reco-admin manages providers with company details, type (direct/consolidation), warehouse address, service regions, platform access flag, and linked tenants.

Purpose: Transport providers must exist before transport can be booked on pickups (plans 05/06 depend on this data).
Output: Provider list, create, and detail/edit pages with Server Actions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pickup-booking-and-transport-management/04-CONTEXT.md
@.planning/phases/04-pickup-booking-and-transport-management/04-RESEARCH.md
@.planning/phases/04-pickup-booking-and-transport-management/04-01-SUMMARY.md

<interfaces>
From packages/db/src/schema/transport.ts (plan 01):
```typescript
export const transportTypeEnum = pgEnum('transport_type', ['direct', 'consolidation'])
export const transportProviders = pgTable('transport_providers', { id, name, contact_email, contact_phone, service_regions, provider_type, warehouse_address, has_platform_access, user_id, active, ... })
export const transportProviderClients = pgTable('transport_provider_clients', { id, transport_provider_id, tenant_id })
```

From packages/db/src/schema/tenants.ts:
```typescript
export const tenants = pgTable('tenants', { id: text('id').primaryKey(), ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create transport provider CRUD Server Actions</name>
  <files>
    apps/web/app/(ops)/transport/providers/actions.ts
  </files>
  <read_first>
    apps/web/app/(ops)/products/actions.ts,
    packages/db/src/schema/transport.ts
  </read_first>
  <action>
Create `apps/web/app/(ops)/transport/providers/actions.ts` with:

1. `'use server'` at top
2. Import auth, db, transportProviders, transportProviderClients, tenants, withRLSContext, eq, revalidatePath, z
3. `requireRecoAdmin()` helper
4. `providerSchema` Zod schema:
   - `name`: z.string().min(1).max(200)
   - `contact_email`: z.string().email().optional().nullable()
   - `contact_phone`: z.string().optional().nullable()
   - `service_regions`: z.string().optional().nullable() (comma-separated country codes)
   - `provider_type`: z.enum(['direct', 'consolidation'])
   - `warehouse_address`: z.string().optional().nullable() (required if provider_type=consolidation — add .refine())
   - `has_platform_access`: z.boolean().default(false)
   - `active`: z.boolean().default(true)
   - `linked_tenant_ids`: z.array(z.string()).min(1, 'At least one client must be linked')

5. `createTransportProvider(formData: FormData)`:
   - requireRecoAdmin()
   - Parse + validate with providerSchema
   - withRLSContext: INSERT into transportProviders, then INSERT into transportProviderClients for each linked_tenant_id
   - revalidatePath('/transport/providers')
   - Return `{ success: true, providerId }`

6. `updateTransportProvider(providerId: string, formData: FormData)`:
   - requireRecoAdmin()
   - Parse + validate
   - withRLSContext: UPDATE transportProviders WHERE id=providerId
   - DELETE existing transportProviderClients WHERE transport_provider_id=providerId, then INSERT new ones
   - revalidatePath('/transport/providers')
   - Return `{ success: true }`
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && npx tsc --noEmit 2>&1 | grep -i "transport.*providers.*actions" || echo "No type errors in transport provider actions"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(ops)/transport/providers/actions.ts contains `'use server'`
    - File contains `export async function createTransportProvider(`
    - File contains `export async function updateTransportProvider(`
    - File contains `withRLSContext(`
    - File contains `transportProviderClients` insert for linked tenants
    - File contains Zod validation with `z.enum(['direct', 'consolidation'])`
  </acceptance_criteria>
  <done>Transport provider CRUD actions exist with tenant linking via join table</done>
</task>

<task type="auto">
  <name>Task 2: Create transport provider list, create, and detail pages</name>
  <files>
    apps/web/app/(ops)/transport/providers/page.tsx,
    apps/web/app/(ops)/transport/providers/new/page.tsx,
    apps/web/app/(ops)/transport/providers/[id]/page.tsx
  </files>
  <read_first>
    apps/web/app/(ops)/products/page.tsx,
    apps/web/app/(ops)/products/new/page.tsx,
    apps/web/app/(ops)/products/[id]/page.tsx,
    apps/web/app/(ops)/transport/providers/actions.ts,
    packages/db/src/schema/transport.ts,
    packages/db/src/schema/tenants.ts
  </read_first>
  <action>
Replicate the product CRUD page pattern for transport providers.

**List page (`providers/page.tsx`):**
1. Server Component, requireAuth(['reco-admin', 'reco'])
2. Fetch all transport providers with their linked tenant count via withRLSContext
3. Table columns: Name, Type (badge: direct/consolidation), Service Regions, Platform Access (yes/no), Active, Linked Clients count
4. "Add Provider" button links to `/transport/providers/new`
5. Each row links to `/transport/providers/[id]`

**Create page (`providers/new/page.tsx`):**
1. Server Component, fetch all tenants for the multi-select
2. Form fields: Name, Contact Email, Contact Phone, Provider Type (select: direct/consolidation), Warehouse Address (shown only when type=consolidation), Service Regions (text), Platform Access (checkbox), Linked Clients (multi-select checkboxes listing all tenants)
3. Submit calls createTransportProvider
4. Use react-hook-form + zod resolver, sonner toast

**Detail page (`providers/[id]/page.tsx`):**
1. Fetch provider by ID with linked tenants
2. Display all fields with inline edit capability (same edit pattern as product detail)
3. Show linked clients list with add/remove
4. Save calls updateTransportProvider
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && npx tsc --noEmit 2>&1 | grep -i "transport.*providers" || echo "No type errors in transport providers pages"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(ops)/transport/providers/page.tsx contains `requireAuth(`
    - apps/web/app/(ops)/transport/providers/page.tsx contains table rendering transport providers
    - apps/web/app/(ops)/transport/providers/new/page.tsx contains form with provider_type select
    - apps/web/app/(ops)/transport/providers/new/page.tsx contains multi-select for linked tenants
    - apps/web/app/(ops)/transport/providers/[id]/page.tsx exists and fetches provider by ID
    - new/page.tsx contains `createTransportProvider` import
  </acceptance_criteria>
  <done>Transport provider registry pages render with list, create, and detail/edit views</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Provider list page accessible at /transport/providers
</verification>

<success_criteria>
- Transport provider CRUD fully functional with Server Actions
- Provider type (direct/consolidation) selectable
- Providers linked to tenants via join table
- Warehouse address shown conditionally for consolidation providers
</success_criteria>

<output>
After completion, create `.planning/phases/04-pickup-booking-and-transport-management/04-04-SUMMARY.md`
</output>
