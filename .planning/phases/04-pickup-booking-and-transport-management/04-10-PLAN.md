---
phase: 04-pickup-booking-and-transport-management
plan: 10
type: execute
wave: 5
depends_on: ["04-05", "04-06", "04-07", "04-08", "04-09"]
files_modified:
  - apps/web/app/(ops)/pickups/[id]/page.tsx
  - apps/web/app/(ops)/transport/outbound/page.tsx
autonomous: true
requirements:
  - PICKUP-08

must_haves:
  truths:
    - "End-to-end pickup flow works: submit -> confirm -> book transport -> picked up -> delivered"
    - "All pages link correctly and status transitions are reflected"
  artifacts:
    - path: "apps/web/app/(ops)/pickups/[id]/page.tsx"
      provides: "Updated pickup detail with transport booking summary and status history"
    - path: "apps/web/app/(ops)/transport/outbound/page.tsx"
      provides: "Updated outbound page with shipment list and delivery actions"
  key_links:
    - from: "apps/web/app/(ops)/pickups/[id]/page.tsx"
      to: "apps/web/app/(ops)/pickups/[id]/book-transport/page.tsx"
      via: "Book Transport link on confirmed pickups"
      pattern: "book-transport"
---

<objective>
Wire together all Phase 4 components: update the pickup detail page to show transport booking summary, add delivery marking actions to the outbound shipment page, and ensure the full status lifecycle flows end-to-end. This is the integration plan that connects the pieces built in plans 02-09.

Purpose: Ensures the complete pickup-to-delivery workflow is connected and all status transitions cascade correctly.
Output: Updated detail pages with transport summaries, delivery actions, and cross-page navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pickup-booking-and-transport-management/04-CONTEXT.md
@.planning/phases/04-pickup-booking-and-transport-management/04-05-SUMMARY.md
@.planning/phases/04-pickup-booking-and-transport-management/04-06-SUMMARY.md
@.planning/phases/04-pickup-booking-and-transport-management/04-07-SUMMARY.md
@.planning/phases/04-pickup-booking-and-transport-management/04-08-SUMMARY.md
@.planning/phases/04-pickup-booking-and-transport-management/04-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance pickup detail page with transport booking summary and full status display</name>
  <files>
    apps/web/app/(ops)/pickups/[id]/page.tsx
  </files>
  <read_first>
    apps/web/app/(ops)/pickups/[id]/page.tsx,
    apps/web/app/(ops)/pickups/actions.ts,
    packages/db/src/schema/transport.ts,
    packages/db/src/schema/pickups.ts
  </read_first>
  <action>
Update `apps/web/app/(ops)/pickups/[id]/page.tsx` to add:

1. **Transport booking summary section** — if a transport_bookings row exists for this pickup:
   - Show transport type (Direct/Consolidation badge)
   - Provider name
   - For direct: destination prison facility name
   - For consolidation: warehouse address, and if in outbound shipment, the outbound shipment reference
   - Transport cost (leg 1): transport_cost_market_to_destination_eur
   - If outbound shipment exists: allocated leg 2 cost from outbound_shipment_pickups.allocated_cost_eur
   - Confirmed pickup date
   - Delivery notes (if any)
   - Proof of delivery link (if proof_of_delivery_path exists)

2. **Status timeline/badge display** — show the full pickup status with a visual indicator:
   - Use a horizontal step indicator or badge list showing: Submitted -> Confirmed -> Transport Booked -> Picked Up -> (At Warehouse -> In Outbound Shipment ->) In Transit -> Delivered
   - Highlight current status
   - For consolidation pickups, show the warehouse/outbound steps; hide for direct

3. **Action buttons refinement**:
   - "Book Transport" link: visible only when status='confirmed' (links to /pickups/[id]/book-transport)
   - "Mark Picked Up" / "Mark In Transit" / "Mark Delivered": visible for reco-admin at appropriate status stages, calling updatePickupStatus from ops/pickups/actions.ts
   - "Cancel" button with reason modal: visible for non-terminal statuses

4. Fetch transport_bookings joined with transport_providers, and optionally outbound_shipment_pickups joined with outbound_shipments, for this pickup in the server component data loading.
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && npx tsc --noEmit 2>&1 | grep -i "pickups.*\\[id\\]" || echo "No type errors in pickup detail"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(ops)/pickups/[id]/page.tsx contains transport booking summary section
    - File fetches transport_bookings data for the pickup
    - File displays transport type, provider name, cost
    - File contains status timeline or step indicator
    - File shows "Book Transport" link conditional on status='confirmed'
    - File displays proof of delivery link when path exists
  </acceptance_criteria>
  <done>Pickup detail page shows complete transport booking summary, status timeline, and context-appropriate action buttons</done>
</task>

<task type="auto">
  <name>Task 2: Add shipment list with delivery action to outbound page and run full test suite</name>
  <files>
    apps/web/app/(ops)/transport/outbound/page.tsx
  </files>
  <read_first>
    apps/web/app/(ops)/transport/outbound/page.tsx,
    apps/web/app/(ops)/transport/outbound/actions.ts,
    packages/db/src/schema/transport.ts
  </read_first>
  <action>
Update `apps/web/app/(ops)/transport/outbound/page.tsx` to add:

1. **Active outbound shipments section** below the warehouse inventory table:
   - Fetch outbound_shipments WHERE status IN ('created', 'in_transit') joined with prison_facilities and transport_providers
   - Table columns: Shipment ID (or auto-numbered), Provider, Destination Prison, Total Pallets, Total Cost, Status, Created At
   - Each row has action buttons:
     - status='created': "Mark In Transit" button
     - status='in_transit': "Mark Delivered" button → calls markOutboundDelivered (which cascades to all linked pickups per TRANS-10)
   - Each row expandable or linked to show included pickups

2. **Completed shipments section** (collapsed/expandable):
   - Fetch outbound_shipments WHERE status='delivered' (last 30 days)
   - Same columns, read-only

3. Ensure the "Create Outbound Shipment" button is prominently placed at the top of the warehouse inventory section

4. Run the full Phase 4 test suite to verify everything integrates:
   ```bash
   pnpm --filter web test --run
   ```
  </action>
  <verify>
    <automated>cd C:/Users/BragiHallsson/Projects/deliverygearrecycling && pnpm --filter web test --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(ops)/transport/outbound/page.tsx contains active outbound shipments table
    - File contains "Mark Delivered" button calling markOutboundDelivered
    - File contains "Mark In Transit" button
    - File fetches outbound_shipments with status filter
    - All tests pass: `pnpm --filter web test --run` exits 0
  </acceptance_criteria>
  <done>Outbound page shows warehouse inventory AND active/completed shipments with delivery marking; full test suite passes</done>
</task>

</tasks>

<verification>
- Full test suite: `pnpm --filter web test --run`
- TypeScript compiles: `npx tsc --noEmit`
- All pages compile and link correctly
</verification>

<success_criteria>
- Pickup detail shows transport booking summary with costs, provider, status
- Outbound page shows warehouse inventory + shipment list with delivery actions
- markOutboundDelivered cascades status to all linked pickups (TRANS-10)
- Full test suite passes
- All Phase 4 pages link together correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-pickup-booking-and-transport-management/04-10-SUMMARY.md`
</output>
