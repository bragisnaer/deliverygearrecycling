---
phase: 08-dashboards-and-esg-metrics
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/esg-calculator.ts
  - apps/web/lib/esg-calculator.test.ts
autonomous: true
requirements: [ESG-01, ESG-02, ESG-03, ESG-05]

must_haves:
  truths:
    - "sumMaterialWeights correctly multiplies actual_quantity by weight_grams and groups by material"
    - "1,000 bike bags produce exactly 943kg polypropylene, 386kg PVC, 294kg polyester (from Wolt seed data)"
    - "calculateReuseRate returns 0 when total is 0, correct percentage otherwise"
    - "serializeEsgCsv produces header row plus one row per material"
  artifacts:
    - path: "apps/web/lib/esg-calculator.ts"
      provides: "Pure ESG calculation functions"
      exports: ["sumMaterialWeights", "calculateReuseRate", "calculateCO2Avoided", "serializeEsgCsv", "MaterialWeightRow"]
    - path: "apps/web/lib/esg-calculator.test.ts"
      provides: "Unit tests anchored to Wolt seed values"
      contains: "943"
  key_links:
    - from: "apps/web/lib/esg-calculator.test.ts"
      to: "apps/web/lib/esg-calculator.ts"
      via: "import { sumMaterialWeights }"
      pattern: "sumMaterialWeights"
---

<objective>
Create the pure ESG calculation engine with TDD: material weight aggregation, reuse rate, CO2 avoided stub, and CSV serialiser.

Purpose: ESG-01 through ESG-05 all depend on pure calculation functions. Building these first with tests anchored to Wolt seed data ensures correctness before any UI or query work.
Output: `esg-calculator.ts` with 4 exported functions and a passing test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboards-and-esg-metrics/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write failing tests for ESG calculator functions</name>
  <files>apps/web/lib/esg-calculator.test.ts</files>
  <read_first>
    - packages/db/src/seed-wolt.ts (lines 183-220 for exact material weights per product)
    - apps/web/lib/discrepancy.test.ts (established test file pattern)
    - apps/web/vitest.config.ts (test configuration)
  </read_first>
  <behavior>
    - sumMaterialWeights with 1000 bike bags: Polypropylene → 943kg, PVC → 386kg, Polyester → 294kg, PE+Aluminium → 296kg, Foam → 292kg, Remains → 260kg, Cotton-polyester → 98kg, Zipper Metal → 54kg, POM → 37kg, Metal Screws → 15kg, Copper → 4kg, Nylon → 1kg
    - sumMaterialWeights with empty array → returns empty array
    - sumMaterialWeights with mixed products (bike bag + car bag lines) → correctly aggregates shared materials (Polyester appears in both: 294g bike + 555g car = summed)
    - sumMaterialWeights returns results sorted by total_weight_kg descending
    - calculateReuseRate(0, 0) → 0
    - calculateReuseRate(1000, 250) → 25.0
    - calculateReuseRate(1000, 333) → 33.3
    - calculateCO2Avoided(any, null) → { value_kg: null, formula_pending: true }
    - serializeEsgCsv with 2 rows → header line "Material,Total Weight (kg),Item Count" + 2 data lines
    - serializeEsgCsv with material name containing comma → properly quoted
  </behavior>
  <action>
    Create `apps/web/lib/esg-calculator.test.ts` with the following test structure:

    1. Import from `@/lib/esg-calculator` (module does not exist yet — tests must fail):
       - `sumMaterialWeights`, `calculateReuseRate`, `calculateCO2Avoided`, `serializeEsgCsv`
       - Type `MaterialWeightRow`

    2. `describe('sumMaterialWeights')`:
       - Test "1000 bike bags produce correct material weights (ESG-02)" using exact values from seed-wolt.ts: `{ actual_quantity: 1000, weight_grams: 943, material_name: 'Polypropylene' }` etc. for all 12 materials. Assert each result row matches expected kg (943, 386, 296, 294, 292, 260, 98, 54, 37, 15, 4, 1).
       - Test "empty input returns empty array"
       - Test "aggregates shared materials across products" with Polyester from bike bag (294g) and car bag (555g) at quantity 100 each → total 84.9kg
       - Test "results sorted descending by total_weight_kg"

    3. `describe('calculateReuseRate')`:
       - Test zero denominator returns 0
       - Test 250/1000 returns 25.0
       - Test 333/1000 returns 33.3

    4. `describe('calculateCO2Avoided')`:
       - Test returns `{ value_kg: null, formula_pending: true }` regardless of input

    5. `describe('serializeEsgCsv')`:
       - Test header row is `Material,Total Weight (kg),Item Count`
       - Test produces correct number of lines (header + data rows)
       - Test material names with commas are double-quoted

    Run tests: `pnpm --filter web test --run esg-calculator` — all must FAIL (module not found).
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run esg-calculator 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/lib/esg-calculator.test.ts` exists
    - File contains `import { sumMaterialWeights` from `@/lib/esg-calculator`
    - File contains test string `943` (Polypropylene anchor value)
    - File contains test string `386` (PVC anchor value)
    - File contains test string `294` (Polyester anchor value)
    - File contains `describe('sumMaterialWeights'` with at least 4 test cases
    - File contains `describe('calculateReuseRate'` with at least 3 test cases
    - File contains `describe('calculateCO2Avoided'`
    - File contains `describe('serializeEsgCsv'`
    - Test command exits non-zero (tests fail because module does not exist)
  </acceptance_criteria>
  <done>Test file exists with 10+ test cases anchored to Wolt seed data; all tests fail because esg-calculator.ts does not exist yet</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement ESG calculator to pass all tests</name>
  <files>apps/web/lib/esg-calculator.ts</files>
  <read_first>
    - apps/web/lib/esg-calculator.test.ts (the tests just written — implementation must pass them)
    - apps/web/lib/discrepancy.ts (established pure function pattern in lib/)
    - packages/db/src/seed-wolt.ts (lines 183-220 for weight verification)
  </read_first>
  <action>
    Create `apps/web/lib/esg-calculator.ts` with these exports:

    1. **Type `MaterialWeightRow`:**
       ```typescript
       export type MaterialWeightRow = {
         material_name: string
         total_weight_kg: number
         item_count: number
       }
       ```

    2. **`sumMaterialWeights(lines)`:** Takes array of `{ actual_quantity: number; weight_grams: number; material_name: string }`. Uses a `Map<string, { weight_kg: number; count: number }>` accumulator. For each line: `kg = (actual_quantity * weight_grams) / 1000`. Accumulates by material_name. Returns `MaterialWeightRow[]` sorted descending by `total_weight_kg`. Use `Math.round(weight_kg * 1000) / 1000` for 3-decimal precision.

    3. **`calculateReuseRate(totalProcessed, reuseProcessed)`:** Returns `0` if `totalProcessed === 0`. Otherwise `Math.round((reuseProcessed / totalProcessed) * 1000) / 10` for one decimal place.

    4. **`calculateCO2Avoided(_materialWeights, _formulaConfig)`:** Stub — always returns `{ value_kg: null, formula_pending: true }`. Parameters: `_materialWeights: MaterialWeightRow[]`, `_formulaConfig: Record<string, number> | null`.

    5. **`serializeEsgCsv(rows)`:** Header: `'Material,Total Weight (kg),Item Count'`. Each row: `"${material_name}",${total_weight_kg},${item_count}` — material_name wrapped in double quotes to handle commas. Returns all lines joined with `'\n'`.

    Run tests: `pnpm --filter web test --run esg-calculator` — all must PASS.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run esg-calculator</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/lib/esg-calculator.ts` exists
    - File exports `sumMaterialWeights` function
    - File exports `calculateReuseRate` function
    - File exports `calculateCO2Avoided` function with `formula_pending: true`
    - File exports `serializeEsgCsv` function
    - File exports type `MaterialWeightRow`
    - `pnpm --filter web test --run esg-calculator` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All ESG calculator tests pass; 4 pure functions exported; no DB dependency in the module</done>
</task>

</tasks>

<verification>
- `pnpm --filter web test --run esg-calculator` passes with all tests green
- `esg-calculator.ts` has zero imports from `@repo/db` or any DB module (pure functions only)
- 1000 bike bags test produces exactly 943kg Polypropylene (ESG-02 anchor)
</verification>

<success_criteria>
- Pure ESG calculation functions exist and are fully tested
- Tests are anchored to real Wolt seed data values
- CO2 avoided is stubbed with formula_pending flag (ESG-04 blocker acknowledged)
- CSV serialiser handles quoted material names
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboards-and-esg-metrics/08-01-SUMMARY.md`
</output>
