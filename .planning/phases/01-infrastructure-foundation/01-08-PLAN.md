---
phase: 01-infrastructure-foundation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/middleware.ts
autonomous: true
gap_closure: true
requirements:
  - ROUTE-01
  - ROUTE-02

must_haves:
  truths:
    - "Next.js executes proxy() on every request matching the config matcher"
    - "x-tenant-context and x-tenant-id headers are injected for all matched routes"
    - "Visiting ops.localhost resolves context=ops; visiting wolt.localhost resolves context=client with tenantSlug=wolt"
  artifacts:
    - path: "apps/web/middleware.ts"
      provides: "Next.js middleware entry point that delegates to proxy()"
      exports: ["middleware", "config"]
  key_links:
    - from: "apps/web/middleware.ts"
      to: "apps/web/proxy.ts"
      via: "named re-export"
      pattern: "export.*proxy.*middleware.*config.*from.*proxy"
---

<objective>
Create apps/web/middleware.ts so Next.js actually executes the subdomain tenant resolution on every request.

Purpose: proxy.ts is a complete and correct implementation of subdomain-based tenant routing, but Next.js only runs middleware when the file is named `middleware.ts` at the app root. Without this file, proxy() is dead code — x-tenant-context and x-tenant-id headers are never injected, making ROUTE-01 and ROUTE-02 unimplemented at runtime despite proxy.ts existing.

Output: apps/web/middleware.ts (two-line file)
</objective>

<execution_context>
@C:/Users/BragiHallsson/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BragiHallsson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From apps/web/proxy.ts — the exports this middleware.ts must re-export -->
```typescript
export function proxy(request: NextRequest): NextResponse

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|sitemap.xml|robots.txt).*)',
  ],
}
```
The `proxy` function reads the `host` header, calls `getTenantFromHost()`, and returns `NextResponse.next()` with `x-tenant-context` and `x-tenant-id` headers set. The `config.matcher` excludes static assets and Next.js internals.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create apps/web/middleware.ts</name>
  <files>apps/web/middleware.ts</files>
  <action>
Create `apps/web/middleware.ts` with exactly these two lines:

```typescript
export { proxy as middleware, config } from './proxy'
```

No other content. No imports. No logic. This single re-export is the entire file.

Do NOT copy the proxy function body into middleware.ts — keep all logic in proxy.ts. The re-export pattern is intentional: proxy.ts remains independently testable (getTenantFromHost is a pure function with its own unit tests) while middleware.ts satisfies Next.js's file-name requirement.

Note: The CONTEXT.md decision log states "no middleware.ts (Next.js 15 pattern)" — this referred to not using the older pattern of combining routing and auth in middleware. proxy.ts was always intended to be the middleware; the missing file was an oversight in plan execution, not a design decision.
  </action>
  <verify>
    <automated>test -f C:/Users/BragiHallsson/Projects/deliverygearrecycling/apps/web/middleware.ts && grep -q "proxy as middleware" C:/Users/BragiHallsson/Projects/deliverygearrecycling/apps/web/middleware.ts && grep -q "config" C:/Users/BragiHallsson/Projects/deliverygearrecycling/apps/web/middleware.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>apps/web/middleware.ts exists, exports `middleware` (aliased from proxy) and `config` from ./proxy, file is two lines or fewer.</done>
</task>

</tasks>

<verification>
After creating middleware.ts:

1. Confirm file exists: `ls apps/web/middleware.ts`
2. Confirm exports: `grep -n "." apps/web/middleware.ts` — should show the single re-export line
3. Confirm TypeScript compiles: `cd apps/web && pnpm tsc --noEmit` — no type errors introduced
4. Confirm Next.js build does not error: `pnpm --filter @repo/web build` (or `pnpm build` from root)

The proxy.ts unit tests already cover the routing logic and do not need to be re-run — this plan only adds the missing entry point.
</verification>

<success_criteria>
- apps/web/middleware.ts exists with exactly: `export { proxy as middleware, config } from './proxy'`
- TypeScript compilation passes with no new errors
- ROUTE-01 truth restored: "proxy.ts resolves tenant context from subdomain without DB call" — proxy() is now invoked by Next.js on every matched request
- ROUTE-02 truth restored: "x-tenant-context and x-tenant-id headers injected for all matched routes"
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-08-SUMMARY.md`
</output>
